# 前端样式优化方案（V5：体验一致性、移动可用性与可持续治理）

> 审查日期：2026-07-28  
> 审查范围：`frontend-app/src`（React 主应用；不含 `canvas-app` 的 Vue 独立工程）  
> 文档定位：本方案承接 V4 的样式架构治理成果，聚焦“用户实际看到的界面”与后续可落地的改造顺序；不在本轮直接修改业务流程或后端接口。

---

## 1. 结论摘要

当前前端已经具备较好的基础治理能力：样式按照功能域拆分、全局 Token 已存在、`!important` 被禁止、标准断点被脚本约束，且样式检查均能通过。下一阶段最有价值的工作不再是大规模机械拆 CSS，而是将治理重点从“文件是否合规”升级为“不同页面、状态和设备上是否一致且可用”。

本次审查发现的首要问题是移动端应用壳层没有完成真正的导航模式切换：在 **375px** 宽度的创作中心中，左侧导航仍占据约 200px，主内容被压缩为不足半屏的窄栏，卡片与图文内容无法正常阅读。与此同时，落地页在 375px 下的主标题发生不自然换行。两项问题都直接影响移动端核心路径，应优先于继续微调单个字号或阴影。

建议采用以下总策略：

1. 先修复 App Shell 的移动端信息架构和关键页面的最小可用宽度。
2. 再统一“工作台、列表、历史记录、弹层、空状态”的视觉基线与组件契约。
3. 用语义化 Token、分层样式和可视化回归测试，防止局部补丁重新引入覆盖冲突。
4. 将每次改造限制在可回归、可验收、可回滚的功能批次内，不做一次性全量重写。

---

## 2. 审查依据与当前基线

### 2.1 已执行的检查

在 `frontend-app` 目录执行，结果均通过：

```powershell
npm.cmd run check:styles
npm.cmd run check:redundancy
npm.cmd run check:breakpoints
```

| 项目 | 当前结果 | 含义 |
| --- | ---: | --- |
| CSS / SCSS 文件数 | 163 | 样式已按功能拆分，规模可控，但入口与职责仍需持续收敛。 |
| 自定义属性（Token/局部变量） | 317 | 已有设计变量基础，下一步应减少同义变量和页面内硬编码。 |
| 原始重复选择器统计 | 405 | 为趋势指标；不能与上下文重复选择器直接比较。 |
| 上下文重复选择器 | 6 | 低于当前阈值，说明大规模无效覆盖已受控制。 |
| `!important` | 0 | 应继续保持为硬性门禁。 |
| 非标准媒体断点 | 0 | 当前仅允许 375 / 768 / 1024 / 1440 / 1920。 |
| 含 10–12px 字号的 CSS 文件 | 86 | 辅助信息密度偏高，需按可读性分级治理。 |
| 直接写数值 `z-index` 的 CSS 文件 | 57 | 存在绕开层级 Token 的风险。 |
| `@media` 规则出现次数 | 165 | 响应式规则分散，适合按“壳层 + 功能模块”重新归类。 |

### 2.2 代码与真实页面观察

本次同时检查了以下内容：

- 全局样式入口：`src/main.jsx`、`src/styles/tokens.css`、`src/styles/base.css`。
- 应用壳层：`src/layouts/appShell.css`、`src/layouts/appShellResponsive.css`、`src/layouts/appShellFeature.css`。
- 高复杂度页面的样式入口：图像、文章、聊天、音乐、资产等功能页。
- 本地运行页面：落地页、创作中心；并在桌面、1024px 平板宽度及 375px 移动宽度下进行视觉核验。

### 2.3 已确认的实际问题

| 优先级 | 问题 | 证据与影响 |
| --- | --- | --- |
| P0 | 移动端仍保留桌面侧栏 | 375px 创作中心中侧栏约占 200px，内容区被挤压，卡片图文无法有效阅读或操作。 |
| P0 | 壳层的 1024px 规则语义冲突 | `appShell.css` 在相同断点先把侧栏宽度设为 200px，后又把 `--feature-sidebar-width` 设为 0；`appShellResponsive.css` 再单独调整侧栏定位。规则分散且意图不清，容易因导入顺序造成回归。 |
| P1 | 落地页窄屏排版缺乏文字策略 | 375px 下“千面创想 一面即达”断为不均衡两行，首屏的视觉重心下降。 |
| P1 | 工作台控件的尺寸与密度不统一 | 多个功能中同时存在 10/11/12/13/14/15/16px 的正文、标签和按钮文字；近期局部字号调整进一步说明缺少明确的“场景级”规范。 |
| P1 | 侧栏、顶部栏、功能页各自维护响应式 | 壳层、聊天、文章、音乐等均有独立断点文件；若没有统一的布局契约，功能页会各自假定侧栏宽度和可用高度。 |
| P1 | 视觉常量仍分散在业务样式中 | Token 已存在，但颜色、白色背景、层级数值、阴影和圆角仍大量直接写在功能 CSS 中，主题和一致性修改成本偏高。 |
| P2 | 全局基础样式的跨平台字体回退不足 | `base.css` 仅指定 `PingFang SC`，Windows、Android 和 Linux 下的中文与拉丁字形表现不可控。 |
| P2 | 滚动条被全局强制定制 | `*` 选择器对所有容器设置滚动条，可能影响代码编辑、画布、弹层和嵌入式控件的可用性与对比度。 |
| P2 | 可访问性已覆盖焦点与减弱动效，但尚未形成状态矩阵 | 当前已有 `focus-visible`、`prefers-reduced-motion` 检查；仍应系统覆盖 loading / empty / error / disabled / long-text / keyboard 的视觉状态。 |

> 说明：未在本轮将“样式文件较多”直接判定为问题。当前单页使用 `*Styles.css` / `*Styles.scss` 作为入口的方式是合理的；真正需要治理的是跨层覆盖、壳层契约不清与同类组件体验不一致。

---

## 3. 优化目标、边界与验收指标

### 3.1 目标

1. **移动端可用**：375px、390px、414px 宽度下，核心导航与核心创作页不出现横向挤压、主内容窄栏、不可触达操作或遮挡。
2. **平板端稳定**：768px、1024px 下，侧栏模式、顶部操作区、内容宽度与滚动归属明确，不依赖偶然的样式导入顺序。
3. **工作台一致**：输入、上传、参数区、生成按钮、结果区、历史卡片、空状态和弹层遵守同一套尺寸与状态规范。
4. **样式可维护**：新增样式不新增全局业务选择器、不新增 `!important`、不以 `fix/override/temp` 文件承接长期规则。
5. **回归可控**：关键路由在 375 / 768 / 1024 / 1440 / 1920 五档宽度均有可执行的视觉或 DOM 回归覆盖。

### 3.2 不在本轮范围

- 不切换至 Tailwind、CSS-in-JS 等新样式技术栈。
- 不借样式改造重写接口、鉴权、生成任务或数据模型。
- 不在没有素材审计的前提下删除图片、视频或字体资源。
- 不改变品牌主色、产品信息架构或文案含义；需要产品决策的视觉改版须另开设计任务。

### 3.3 建议 KPI

| 维度 | 本轮完成标准 |
| --- | --- |
| 移动可用性 | 创作中心、图片生成、聊天、文章、音乐等核心入口在 375px 下主内容宽度不少于 `calc(100vw - 24px)`，无常驻侧栏挤压。 |
| 触控目标 | 主操作与导航项可点击区域不小于 40×40px；移动端优先达到 44×44px。 |
| 排版 | 正文默认不低于 14px；12px 仅用于次要辅助信息；10/11px 仅限徽标、时间戳等非关键文本，并需设计说明。 |
| 层级 | 新增浮层只使用 `--z-*` Token；清理本轮触达文件中的裸 `z-index`。 |
| 样式门禁 | 保持 `!important = 0`、非标准断点 = 0、上下文重复选择器不高于当前阈值。 |
| 视觉回归 | 每个关键路由至少覆盖 editing、loading、success、empty/error 中适用的状态。 |

---

## 4. 目标样式架构

### 4.1 固定层次与职责

建议将现有约定明确为以下层次。层间只允许向下依赖，禁止功能样式反向覆盖全局基础层。

```text
tokens
  └─ 原始色彩、语义颜色、尺寸、层级、动效、断点变量
base
  └─ 重置、字体、焦点、图片、表单基础、减少动效
layout
  └─ App Shell、全局容器、导航模式、页面可用高度
ui components
  └─ Button、Field、Tabs、Card、Dialog、FeedbackState、HistoryCard
feature
  └─ article / image / video / chat / music / assets 等业务外观
compatibility（极少数）
  └─ 有来源、范围、删除条件的第三方兼容规则
```

落实原则：

- `src/main.jsx` 只导入 Token、Base、通用 UI 样式，不导入任一 feature 样式。
- 每个页面 JSX 只导入所在功能的单一样式入口，例如 `imageGenerationStyles.css`。
- 样式入口仅编排 `@import` 顺序，不再堆放临时选择器。
- 所有业务选择器必须带功能根节点，例如 `.image-gen-view`、`[data-feature-module="article"]`；禁止在全局文件中新增 `.panel`、`.header`、`.button` 一类通用但无范围的业务规则。
- 允许使用 `@layer base, layout, components, features, compat;` 作为导入顺序的第二道保护；在改造首批页面验证兼容性后逐步启用，避免一次性改写全部样式。

### 4.2 Token 重构：从“变量集合”到“语义契约”

当前 `tokens.css` 已包含颜色、间距、圆角、阴影、字号、层级和动效。下一步不应盲目增加 Token 数量，而应清理语义重复，并为常用组件建立稳定契约。

建议新增或统一的语义 Token：

```css
:root {
  /* Typography */
  --font-family-sans: "PingFang SC", "Microsoft YaHei", "Noto Sans SC",
    -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
  --font-size-body: 14px;
  --font-size-body-lg: 16px;
  --font-size-meta: 12px;
  --line-height-body: 1.5;

  /* Component sizing */
  --control-height-sm: 32px;
  --control-height-md: 36px;
  --control-height-lg: 44px;
  --touch-target-min: 44px;

  /* Layout */
  --page-gutter-desktop: 24px;
  --page-gutter-tablet: 16px;
  --page-gutter-mobile: 12px;
  --content-reading-max: 760px;
  --content-workbench-max: 1440px;
}
```

Token 使用约束：

| 类别 | 允许直接写值的位置 | 功能样式中的要求 |
| --- | --- | --- |
| 品牌/中性色/状态色 | 仅 Token 文件 | 用语义 Token，不直接写十六进制或 `rgba()`。 |
| 间距/圆角/阴影 | Token 文件 | 优先复用既有刻度；确有业务尺寸时以组件语义命名局部变量。 |
| `z-index` | Token 文件 | 只用 `--z-raised`、`--z-dropdown`、`--z-modal` 等。 |
| 预览比例/媒体高度 | feature 文件 | 可直接声明，但变量名须体现业务含义，如 `--video-preview-ratio`。 |
| 动效 | Token 文件或组件局部 | 只对 `transform`、`opacity` 等低成本属性做短时过渡；必须提供减少动效降级。 |

---

## 5. 响应式与信息架构优化（最高优先级）

### 5.1 断点职责

保持项目现有五档标准宽度，不增加 600、640、820 等临时断点。不同断点不是简单缩小，而是应触发明确的布局模式变化。

| 宽度 | 布局模式 | 侧栏策略 | 内容策略 |
| --- | --- | --- | --- |
| ≥1440px | 宽桌面 | 220px 常驻侧栏 | 工作台可使用多列，内容最大宽度受控。 |
| 1024–1439px | 桌面/横向平板 | 200px 常驻或可折叠侧栏 | 缩小间距与次要信息，避免压缩主要编辑区。 |
| 768–1023px | 平板 | 默认折叠；通过菜单/抽屉临时展开 | 工作台两列按页面能力降为主列 + 次级抽屉或上下布局。 |
| 376–767px | 手机 | 不常驻；使用顶部菜单、底部导航或全屏抽屉 | 单列主流程，操作区固定在合理位置，横向区域可显式滚动。 |
| ≤375px | 紧凑手机 | 与手机模式一致，不再额外保留桌面侧栏 | 允许标题和标签适度收缩，但不通过压缩内容区解决问题。 |

### 5.2 App Shell 改造要求

涉及文件：`src/layouts/appShell.css`、`src/layouts/appShellResponsive.css`、`src/layouts/AppShell.jsx`、`src/layouts/AppSidebar.jsx`、`src/layouts/AppHeader.jsx`。

1. **合并壳层响应式规则**：将 App Shell 的断点规则收敛到一个文件，避免同一 1024px 条件在多个文件中反复定义侧栏宽度、定位和 CSS 变量。
2. **引入明确的侧栏模式属性**：由 React 根据视口与用户操作设置 `data-sidebar-mode="rail|collapsed|drawer"`，CSS 只根据模式渲染，不再通过相互覆盖的宽度变量推断状态。
3. **移动端抽屉化**：当宽度小于 768px 时，侧栏默认不占文档流宽度；点击顶部菜单后以 `--z-drawer` 抽屉层显示，背景使用 `--overlay-backdrop`，关闭后把焦点还给触发按钮。
4. **内容区保障**：移动端 `.feature-main` 宽度必须以 `100%` 为基准，左右内边距使用 `--page-gutter-mobile`，禁止依赖剩余宽度计算获得主内容空间。
5. **滚动归属唯一化**：桌面端由 `.feature-main` 承担纵向滚动；移动端如果顶部栏参与正常流，必须验证不会形成 body、shell、feature 三层竞争滚动。
6. **无限画布例外显式声明**：无限画布的全视口规则仅绑定 `[data-active-nav="infinite-canvas"]`，并在其他功能页禁止继承该 `overflow: hidden` 行为。

### 5.3 落地页窄屏排版

涉及文件：`src/StudioLandingHero.css`、`src/StudioLandingContentSections.css`、`src/StudioLandingFooter.css`。

- Hero 标题使用 `font-size: clamp(2rem, 9vw, 4.5rem)`、合理行高与 `text-wrap: balance`（同时保留不支持浏览器的自然换行），避免仅在 375px 出现孤立单字行。
- 统计卡在 768px 以下应转为 2×2 网格或横向滚动，不能依赖四列强塞。
- 导航项在 1024px 以下隐藏前，应提供等价入口（折叠菜单或“探索”按钮）；不能只保留品牌和创作中心而丢失页内导航。
- 落地页卡片标题、描述与按钮保持 44px 触控目标；视觉卡片可缩小，但不可让文字行宽低于约 12 个汉字。

---

## 6. 工作台与组件视觉规范

### 6.1 统一页面骨架

所有生成类页面（图片、视频、文章、数字人、音乐、语音、换脸、去水印等）采用统一的区域顺序：

```text
页面标题 / 面包屑（可选）
├─ 工作台顶部操作：任务入口、积分、历史、帮助
├─ 主编辑区：提示词 / 上传 / 参数 / 生成按钮
├─ 任务反馈区：排队 / 生成中 / 失败 / 成功
└─ 结果与历史区：预览、下载、复用、删除、分页或瀑布流
```

共用规则：

- 一级页面标题：20–24px；模块标题：16–18px；正文：14px；辅助：12px。
- 主按钮使用 `--control-height-lg`；次级输入与选择器使用 `--control-height-md`；图标按钮触控盒至少 40px。
- 卡片圆角、边框、阴影只允许使用标准 Token；结果卡片与配置卡片不得各自定义“近似但不同”的灰色边框。
- “生成中”使用稳定的布局占位，避免状态切换使按钮、预览区和历史列表跳动。
- 长任务列表、历史列表、瀑布流均应有骨架屏、空状态、失败重试和加载更多状态。

### 6.2 表单与上传组件

优先建立可复用的 `Field`、`PromptComposer`、`UploadDropzone`、`SegmentedTabs`、`AspectRatioPicker` 外观契约，而非复制每个功能中的输入框规则。

| 组件 | 应统一的内容 | 特殊规则 |
| --- | --- | --- |
| `Field` | 标签、必填标识、帮助文本、错误文本、间距 | 错误提示关联 `aria-describedby`；不能只依赖边框变红。 |
| `PromptComposer` | 容器、占位符、计数器、快捷操作、发送/生成按钮 | 多行输入在移动端保留至少 96px 高度，键盘弹出时主按钮仍可见。 |
| `UploadDropzone` | 空态、拖拽态、上传中、成功预览、删除操作 | 文件类型与大小说明保持可见；移动端不只依赖拖拽交互。 |
| `Tabs` | 激活态、悬浮态、焦点态、禁用态、溢出滚动 | 横向滚动需保留当前激活项可见，不使用缩小到难点击的文字。 |
| `ResultCard` | 媒体比例、操作栏、状态徽标、长文本截断 | 媒体加载失败提供占位与重试，不显示空白卡片。 |

### 6.3 历史记录与资产列表

近期已对语音历史卡片比例与间距进行调整，建议将此类规则提升为统一策略：

- 封面型历史卡片默认使用 `16 / 9`；方图、头像、音频波形等例外以组件 modifier 明确声明。
- 卡片操作统一采用“悬浮显示 + 键盘聚焦可见”的策略；移动端操作可常驻或放入更多菜单，不依赖 hover。
- 列表密度分为舒适/紧凑两档，不再由各功能单独调 `gap`、字号与封面高度。
- 在 375px 和 768px 下分别明确一列、两列或横向滚动策略；瀑布流不能借由极窄列数维持桌面布局。

### 6.4 弹层、抽屉与反馈状态

- Dialog、Drawer、Popover、Tooltip、Toast 均使用 `--z-*` Token；不得自行定义 `z-index: 1200` 等魔法数。
- 弹层宽度使用 `min(设计宽度, calc(100vw - 2 * var(--page-gutter-mobile)))`，移动端不要固定桌面宽度。
- 所有关闭按钮须有可读标签与焦点样式；Esc、遮罩点击、焦点圈回收行为一致。
- 建立 `FeedbackState` 使用规则：`loading`（任务反馈）、`empty`（无结果）、`error`（失败与重试）、`success`（完成和下一步）四类状态使用同一基础组件。

---

## 7. 可访问性、性能与跨平台细节

### 7.1 可访问性

1. 将 `base.css` 的字体族升级为跨平台回退栈，避免非 Apple 平台退回到不可控默认字体。
2. 保留现有 `:focus-visible` 规范，并为自定义 Tabs、卡片按钮、侧栏抽屉触发器补齐键盘焦点和激活态。
3. 文字对比度以 WCAG AA 为最低目标：正文与背景至少 4.5:1，大号标题至少 3:1；浅灰辅助文字不应承载关键操作说明。
4. 逐步把 10/11px 文本收敛到徽标、时间戳、非关键元数据；错误、帮助、表单标签和可点击文本至少 12–14px。
5. 所有只在 hover 出现的内容，在触摸屏和键盘下提供可发现的替代交互。

### 7.2 性能与渲染稳定性

- 将全局滚动条样式从 `*` 限定为主滚动容器；画布、代码/富文本编辑器、第三方控件采用各自兼容规则。
- 对大型结果列表使用 `content-visibility: auto`、图片 `loading="lazy"`、明确 `aspect-ratio` 与占位尺寸，降低滚动抖动（CLS）。
- 动效仅使用 `opacity`、`transform`，避免频繁触发布局与阴影动画；在 `prefers-reduced-motion` 下关闭非必要装饰性动效。
- 弹层中的 `backdrop-filter` 仅用于少数关键层，低性能设备使用纯色回退。
- 视觉资源优化另立任务：先建立路由引用清单和首屏资源预算，再处理视频转码、懒加载与 CDN，不和本方案的 CSS 重构混在同一批次。

---

## 8. 分阶段实施计划

### 阶段 A：移动端止血（P0，建议 1–2 个工作日）

**目标**：让 375px / 390px / 414px 下的创作中心与核心入口可正常使用。

1. 收敛 App Shell 的 1024px、768px 规则，删除相互覆盖的侧栏宽度定义。
2. 实现移动端导航抽屉：默认关闭、菜单触发、遮罩关闭、Esc 关闭、焦点回收。
3. 保障 `.feature-main` 移动端全宽单列；修复创作中心内容被 200px 侧栏挤压的问题。
4. 修复落地页 Hero 标题、统计区域与导航在 375px 的换行及密度。
5. 对图片、文章、聊天三个高频页面做 375px 冒烟测试，确认壳层修复不会导致页面横向溢出。

**验收**：移动端截图中不存在常驻桌面侧栏；主内容可阅读、主按钮可点击；浏览器控制台无布局相关错误。

### 阶段 B：壳层与通用控件收敛（P1，建议 3–5 个工作日）

**目标**：减少跨功能重复规则，建立后续页面改造的稳定底座。

1. 统一 `AppHeader`、`AppSidebar`、`FeatureModuleShell` 的尺寸、滚动和层级规则。
2. 定义按钮、输入、上传、Tabs、卡片、状态反馈的 CSS 契约及最小触控尺寸。
3. 整理 `tokens.css`：补充语义字体、控件高度、页面边距 Token；对新增/触达文件替换裸颜色与裸层级值。
4. 将全局滚动条规则缩小作用域，审查画布、弹层、长列表的滚动体验。
5. 为上述组件建立 Story/测试页或最小演示路由，作为样式验收基准。

**验收**：至少 5 个功能页面复用同一套控件规则；触达文件不再新增裸 `z-index` 和硬编码主题颜色。

### 阶段 C：高频工作台体验统一（P1，建议 1–2 周）

**优先顺序**：图片生成 → 文章生成 → 聊天 → 视频生成 → 音乐/语音 → 数字人。

对每个功能按以下固定模板改造，避免按文件大小随机处理：

1. 盘点页面状态：editing、uploading、generating、success、empty、error、disabled。
2. 用统一组件替换重复的表单、上传、结果卡片和历史卡片外观。
3. 拆出功能根节点内的 `layout / form / states / results / responsive` 文件，再由一个入口编排顺序。
4. 修正 375、768、1024 下的列数、按钮位置、横向溢出和长文本截断。
5. 给每页补齐视觉回归用例与状态截图。

**验收**：同类操作在不同工作台中的主按钮高度、输入反馈、错误样式、卡片操作和历史密度一致。

### 阶段 D：样式债务清理（P2，持续进行）

1. 对保留 `compat`、`legacy`、`override` 语义的规则建立清单，注明来源、范围、替代方案和删除条件。
2. 每次功能改造仅清理触达区域的冗余规则，不进行无验证的全仓格式化或大范围删样式。
3. 将直接颜色、层级、字号值逐批替换为 Token；避免为了“Token 化”新增大量同义变量。
4. 对 86 个含 10–12px 字号的文件按文本角色分类，优先处理可点击文字、错误提示和移动端关键说明。

---

## 9. 回归测试与质量门禁

### 9.1 页面与状态矩阵

| 页面 | 375px | 768px | 1024px | 1440px | 必测状态 |
| --- | :---: | :---: | :---: | :---: | --- |
| 落地页 | ✓ | ✓ | ✓ | ✓ | 首屏、导航、统计卡、页脚 |
| 创作中心 | ✓ | ✓ | ✓ | ✓ | 侧栏关闭/打开、卡片、灵感流 |
| 图片生成 | ✓ | ✓ | ✓ | ✓ | 编辑、上传、生成中、结果、失败 |
| 文章生成 | ✓ | ✓ | ✓ | ✓ | 表单、模板、参考上传、结果 |
| 聊天 | ✓ | ✓ | ✓ | ✓ | 空会话、长消息、输入、历史抽屉 |
| 视频/音乐/语音 | ✓ | ✓ | ✓ | ✓ | 参数、进度、媒体预览、历史 |
| 无限画布 | — | ✓ | ✓ | ✓ | 全视口、工具栏、弹层、滚动锁定 |

### 9.2 自动化要求

每一批改动至少执行：

```powershell
npm.cmd run check:mojibake
npm.cmd run check:coverage
npm.cmd run check:redundancy
npm.cmd run check:styles
npm.cmd run check:breakpoints
npm.cmd run check:style-entrypoints
npm.cmd run test:routes
npm.cmd run build
```

新增建议：

- 在 Playwright 中为 375 / 768 / 1024 / 1440 建立 viewport 项目配置，或在现有用例中参数化测试。
- 对 P0 页面保存关键状态截图；视觉差异由人工确认后再更新基线，避免自动接受回归。
- 增加静态检查：禁止在 feature CSS 中新增无 Token 的 `z-index`；允许名单仅限第三方隔离样式并要求注释原因。
- 增加 `overflow` 冒烟断言：`document.documentElement.scrollWidth <= window.innerWidth`，覆盖移动端核心路由。

### 9.3 发布前人工检查清单

- [ ] 375px 下从落地页进入创作中心后，主内容不被侧栏压缩。
- [ ] 移动端侧栏打开、关闭、遮罩点击、Esc、键盘焦点均正常。
- [ ] 关键按钮未被固定栏、软键盘或弹层遮挡。
- [ ] 横向 Tabs 与参数组可滚动，但页面主体不产生意外横向滚动。
- [ ] loading、error、empty 状态在浅色背景下具有足够对比度。
- [ ] 鼠标 hover 才出现的操作在触摸与键盘场景仍可使用。
- [ ] 画布全屏模式与普通工作台切换后，滚动和高度恢复正确。

---

## 10. 风险与实施原则

| 风险 | 应对措施 |
| --- | --- |
| 壳层改动影响所有功能页 | 阶段 A 先只修改壳层与 3 个高频冒烟页；分支内完整跑路由回归后再扩展。 |
| 统一控件导致个别业务布局变化 | 通过组件 modifier 表达确有差异的业务需求，不以局部覆盖重新分叉基础规则。 |
| Token 迁移造成视觉色差 | 先建立原值到语义 Token 的映射表；每个批次限制在一个功能域并进行截图比对。 |
| 移动抽屉影响无障碍 | 在实现前明确焦点陷阱、Esc、返回焦点和背景不可交互策略，并在键盘下验收。 |
| 大范围清理引入隐藏回归 | 禁止“顺手删除”未覆盖的样式；每一删除项必须能定位到组件、路由或视觉测试。 |

---

## 11. 推荐的首个实施任务拆分

建议以一个可独立验收的 PR 开始：**“修复移动端 App Shell 导航与创作中心主内容挤压”**。

| 子任务 | 主要文件 | 产出 |
| --- | --- | --- |
| 梳理壳层状态 | `AppShell.jsx`、`AppSidebar.jsx`、`AppHeader.jsx` | 明确 desktop / tablet / mobile 的导航状态与属性。 |
| 收敛壳层 CSS | `appShell.css`、`appShellResponsive.css` | 一个断点只在一处定义；移动端无常驻侧栏。 |
| 实现移动抽屉 | 壳层组件及就近样式 | 菜单触发、遮罩、关闭与焦点行为完整。 |
| 修复落地页窄屏 | `StudioLandingHero.css` 等 | 标题平衡换行、统计与导航适配。 |
| 补回归 | `frontend-app/tests` | 375px 创作中心、落地页及核心工作台的无横向溢出断言与截图。 |

完成该 PR 后，再进入“通用控件 Token 化”和“高频工作台状态统一”。这样可以先解决用户在手机上无法使用创作中心的直接问题，再逐步降低全站样式维护成本。

