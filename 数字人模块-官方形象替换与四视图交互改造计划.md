# 数字人模块：官方形象替换与四视图交互改造计划

## 1. 本次目标

1. 将当前 12 个旧“官方形象”完整替换为本次生成的 15 个数字人视频。
2. 每个官方数字人建立独立且不可混用的资产关系：
   - 预览视频：本次生成的约 4 秒视频，带原始声音；
   - 正面参考照：`D:\二手车生视频功能素材` 对应素材文件夹中的单人正面近景图；
   - 四视图：复用 `D:\usedCarPlatform\src\assets\数字人形象` 中已裁好的正视、侧视、背视、面部特写四张图。
3. 用户点击官方数字人后：
   - 左侧显示并播放该数字人的视频，允许播放声音；
   - 右侧显示该数字人的四视图，固定为两列两行；
   - 弹窗中不再选择、试听或自动匹配音色；
   - 点击“确认使用”只确认数字人，不修改下方已经选择的音色、语速和情感。
4. 用户发起生成时，后端必须按数字人 ID 取得该数字人的正面参考照，不能再把四视图拼板当作生成参考图。

## 2. 已核实的当前实现与问题

- 官方数字人来自 `backend/src/modules/digital-human/digitalHuman.data.js`，目前有 12 项，字段主要是 `cover`、`assetPath`、`poster`、`threeView`。
- 前端接口不可用时会回退到 `frontend-app/src/data/faceminiData.js` 的旧官方形象，因此后端数据和前端回退数据必须同步替换。
- 形象确认弹窗位于 `frontend-app/src/features/digital-human-v2/components/AvatarConfirmOverlay.jsx`：
  - 左侧视频当前为 `muted + autoPlay + loop`；
  - 右侧嵌入了完整 `VoicePickerPanel`；
  - 弹窗打开时会自动匹配音色，并把语速重置为 `1`、情感重置为“中性”；
  - 确认时还会校验音色。这些行为都与新交互冲突。
- 后端 `getAvatarIdentityImageFile()` 当前优先使用 `avatar.threeView`，实际生成会把四视图拼板作为身份参考图；这必须改为显式的正面照字段优先。
- `frontend-app` 和 `backend` 的静态资产来自同一套 `frontend-app/public` 内容；后端镜像构建时也会复制该目录，因此资产应落入仓库，不能在运行时依赖外部 D 盘素材目录。
- 当前工作区已有未提交修改，且 `AvatarLibraryPanel.jsx`、`DigitalHumanV2View.jsx`、路由截图测试等文件与本改造有交集。实施时只做窄范围补丁，保留现有改动，不重置工作区。

## 3. 已确认的 15 项资产映射

四视图编号规则：`(1)=正视图`、`(2)=侧视图`、`(3)=背视图`、`(4)=面部图`。

| 新官方形象 | 素材文件夹 | 预览视频 | 正面参考照 | 四视图来源 |
|---|---|---|---|---|
| 数字人 5｜车场介绍 | 素材1-车场介绍 | `8月7日.mp4` | `数字人模板(1).png` | 数字人5(1)～(4) |
| 数字人 6｜规模现车 | 素材2-车场介绍 | `数字人视频-MiniMax-H3-精品酒店空中酒廊-4s.mp4` | `数字人(1).png` | 数字人6(1)～(4) |
| 数字人 7｜库存更新 | 素材3-车场介绍 | `样片-MiniMax-H3-参考图+视频-雨后展场-4s.mp4` | `数字人(1).jpg` | 数字人7(1)～(4) |
| 数字人 8｜规模现车 | 素材4-车场介绍 | `样片-MiniMax-H3-参考图+视频-书店咖啡馆-4s.mp4` | `数字人(1).png` | 数字人8(1)～(4) |
| 数字人 9｜精品车源 | 素材5-车场介绍 | `数字人视频-MiniMax-H3-书店咖啡馆-4s.mp4` | `数字人(1).jpg` | 数字人9(1)～(4) |
| 数字人 10｜售后保障 | 素材6-车场介绍 | `数字人视频-MiniMax-H3-现代艺术画廊-4s.mp4` | `图片(1).png` | 数字人10(1)～(4) |
| 数字人 11｜海外售后 | 素材7-车场介绍 | `数字人视频-MiniMax-H3-植物园温室-4s.mp4` | `数字人(1).png` | 数字人11(1)～(4) |
| 数字人 12｜服务保障 | 素材8-车场介绍 | `数字人视频-MiniMax-H3-屋顶花园-4s.mp4` | `数字人(1).png` | 数字人12(1)～(4) |
| 数字人 13｜全天沟通 | 素材9-车场介绍 | `数字人视频-MiniMax-H3-花卉市集-4s.mp4` | `数字人(1).png` | 数字人13(1)～(4) |
| 数字人 14｜出口海外 | 素材10-单车介绍 | `数字人视频-MiniMax-H3-黑胶唱片店-4s.mp4` | `数字人(1).png` | 数字人14(1)～(4) |
| 数字人 15｜外观成色 | 素材11-单车介绍 | `数字人视频-MiniMax-H3-博物馆中庭-4s.mp4` | `数字人(1).jpg` | 数字人15(1)～(4) |
| 数字人 16｜动力工况 | 素材12-单车介绍 | `数字人视频-MiniMax-H3-海边木栈道-4s.mp4` | `数字人(1).jpg` | 数字人16(1)～(4) |
| 数字人 17｜内饰配置 | 素材13-单车介绍 | `数字人视频-MiniMax-H3-复古电影院-4s.mp4` | `数字人(1).jpg` | 数字人17(1)～(4) |
| 数字人 18｜检测透明 | 素材14-单车介绍 | `数字人视频-MiniMax-H3-陶艺工作室-4s.mp4` | `数字人(1).jpg` | 数字人18(1)～(4) |
| 数字人 19｜来源透明 | 素材15-单车介绍 | `数字人视频-MiniMax-H3-精品酒店大堂-4s.mp4` | `数字人(1).jpg` | 数字人19(1)～(4) |

映射核验方式：已将每个素材文件夹中的四视图合成板与 `usedCarPlatform` 的数字人5～19逐一做图像相似度比对，15 项均按顺序唯一命中；部分 PNG 为完全相同文件，JPG 项为同图重新编码。正面参考照始终以素材文件夹中的单人近景图为准，不以四视图的 `(4)` 面部切图替代。

## 4. 数据与资产设计

### 4.1 仓库内资产目录

新增独立目录，避免与旧角色重名：

```text
frontend-app/public/assets/digital-human/official-v2/
  dh-05/
    preview.mp4
    poster.jpg
    reference-front.png|jpg
    views/
      front.png|jpg
      side.png|jpg
      back.png|jpg
      face.png|jpg
  ...
  dh-19/
```

- `preview.mp4`：只使用上表中的新生成成片，不误用素材原始参考视频。
- `poster.jpg`：从对应预览视频提取一帧，仅用于卡片和视频加载前展示。
- `reference-front.*`：来自素材文件夹的单人正面近景图，是生成链路的身份参考图。
- `views/*`：来自 `usedCarPlatform` 已裁好的四张图，只用于确认弹窗展示。
- 复制后生成并校验一份静态清单：15 个视频、15 张正面参考照、60 张四视图、15 张海报，路径和 ID 必须唯一。

### 4.2 官方数字人数据结构

在每个 `publicAvatars` 项中明确区分用途：

```js
{
  id: "official-dh-05",
  name: "数字人 5｜车场介绍",
  status: "ready",
  cover: "/assets/digital-human/official-v2/dh-05/preview.mp4",
  assetPath: "/assets/digital-human/official-v2/dh-05/preview.mp4",
  poster: "/assets/digital-human/official-v2/dh-05/poster.jpg",
  referenceImage: "/assets/digital-human/official-v2/dh-05/reference-front.png",
  views: [
    { type: "front", label: "正视图", url: ".../views/front.png" },
    { type: "side", label: "侧视图", url: ".../views/side.png" },
    { type: "back", label: "背视图", url: ".../views/back.png" },
    { type: "face", label: "面部图", url: ".../views/face.png" },
  ],
}
```

设计原则：视频预览、视频海报、生成参考照、四视图分别使用独立字段，不再用 `cover`、`poster` 或 `threeView` 猜测用途。

## 5. 实施步骤

### 阶段 A：整理、复制和校验资产

1. 按第 3 节映射复制 15 个生成视频和 15 张正面参考照。
2. 复制数字人5～19各自的四张已裁视图，并改为语义化文件名。
3. 从每个预览视频提取海报图；不修改原素材目录中的任何文件。
4. 校验文件数量、图片可解码、视频包含音频流、视频时长和 ID 唯一性。

### 阶段 B：替换官方形象数据

1. 重写 `backend/src/modules/digital-human/digitalHuman.data.js` 的 `publicAvatars` 为 15 项新数据。
2. 同步更新 `frontend-app/src/data/faceminiData.js` 的官方形象回退列表，确保后端暂时不可用时也不会出现旧形象。
3. 保留“我的形象”和 AI 自定义形象逻辑，不影响用户资产。
4. 清除所有旧官方角色静态引用；旧文件可先保留一轮以避免其它页面未知引用报错，确认无引用后再单独决定是否删除。

### 阶段 C：修正真实生成参考图链路

1. 修改 `backend/src/modules/digital-human/digitalHuman.service.js`：
   - `getAvatarIdentityImageFile()` 优先读取 `avatar.referenceImage`；
   - `threeView` 仅作为旧数据兼容回退，不再用于新官方形象；
   - 场景图逻辑仍与身份图逻辑分离，避免把正面照同时误当场景图。
2. 前端生成请求仍只提交 `avatarId`；后端按受控映射取得正面照，避免客户端传任意参考图路径。
3. 日志中记录所用官方数字人 ID 和参考图文件名，便于后续排查串图，但不记录外部签名 URL 或密钥。

### 阶段 D：重做形象确认弹窗

涉及：

- `frontend-app/src/features/digital-human-v2/components/AvatarConfirmOverlay.jsx`
- `frontend-app/src/features/digital-human-v2/components/AvatarLibraryPanel.jsx`
- `frontend-app/src/features/digital-human-v2/digitalHumanAvatarConfirm.css`

具体改法：

1. 删除弹窗内 `VoicePickerPanel`、音色试听接口、音色校验、自动匹配音色，以及重置语速/情感的副作用。
2. `AvatarConfirmOverlay` 只接收 `avatar`、`onClose`、`onConfirm`；确认事件只返回该数字人。
3. 左侧视频：
   - 使用新生成视频；
   - 去掉强制静音，提供原生播放、音量、进度和全屏控制；
   - 默认不依赖“有声自动播放”，因为浏览器通常会拦截；用户点击播放后直接有声；
   - 切换数字人或关闭弹窗时暂停并归零，避免后台串音。
4. 右侧四视图：
   - 标题“数字人四视图”；
   - 固定顺序：正视、侧视、背视、面部；
   - CSS Grid 固定两列，形成上两张、下两张；
   - 使用 `object-fit: contain`，不裁掉人物；
   - 每张图带序号和中文标签；缺图时显示明确占位，不拿其它数字人的图补位。
5. 弹窗建议宽度约 `920px`：桌面端左侧视频约 36%～40%，右侧四视图占剩余空间；窄屏改为上下布局，但四视图仍保持两列。
6. 底部只保留“重新选形象”和“确认使用”；确认后下方音色、语速、情感保持用户当前值不变。

### 阶段 E：测试、视觉验收和运行验证

1. 自动测试：
   - 官方形象恰好 15 项，ID、视频、参考照、四视图路径均唯一；
   - 每项恰好四张视图，顺序固定；
   - 后端身份图解析优先 `referenceImage`，不会选中 `threeView`、视频海报或视频帧；
   - 确认弹窗不触发音色 API、不修改音色/语速/情感；
   - 更新数字人头像库相关单元测试与路由截图测试。
2. 构建验证：运行后端测试、前端 `test:unit`、前端完整构建和静态资产审计。
3. 浏览器视觉验收：
   - 逐个打开 15 个数字人，核对左侧人物与右侧四视图为同一人；
   - 检查视频可播放声音、关闭后无残留音频；
   - 检查 2×2 排版、标签、桌面与窄屏布局；
   - 随机选择至少 3 个数字人，确认下方音色不被覆盖。
4. 生成链路验证：先做不提交供应商任务的请求构造/本地路径检查；如需真实生成验收，再单独征得一次付费任务授权，并核对最终供应商请求使用的是对应 `reference-front`。

## 6. 预计改动范围

- 新增静态资产：约 105 个文件（15 视频 + 15 海报 + 15 正面参考照 + 60 四视图）。
- 后端核心文件：2～3 个数据/服务/测试文件。
- 前端核心文件：4～6 个数据、弹窗、样式和测试文件。
- 不改数据库表，不迁移用户数据，不修改“我的形象”逻辑，不在本轮调整下方音色模块本身。

## 7. Review 后的执行顺序

按 `资产与映射 → 后端参考图链路 → 前端数据回退 → 弹窗交互 → 自动测试 → 浏览器逐项验收` 执行。任何一项人物映射校验失败都停止进入下一阶段，避免把串图带入生成链路。
