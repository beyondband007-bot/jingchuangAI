# Facemini 研发日报

**日期：** 2026-06-25  
**分支：** `fix`  
**统计范围：** 当日已提交代码，不包含合并提交和当前工作区未提交改动

## 一、今日概况

- 完成 16 个非合并提交，其中当前 Git 用户 `Saber1013` 提交 5 个。
- 今日工作覆盖数字人、爆款图文、统一计费、账户体系、个人中心、创作中心和生成页面体验。
- 晚间重点完成数字人 V2 全流程，并将数字人生成最终统一切换至 KIE 可灵模型。
- 已补充数字人前后端改动与后端对接文档，便于后续服务端调整和联调。

## 二、今日完成

### 1. 数字人能力升级

- 新增数字人 V2 工作台，完成官方形象、个人形象、文案、音色、语速、情绪、预览和生成流程。
- 新增照片数字人工作台，支持人物肖像上传、文本驱动和音频驱动。
- 新增数字人统一历史页，可集中查看数字人形象和照片数字人的任务记录。
- 支持任务预览、下载、删除和重新生成。
- 增加个人形象上传、形象库筛选、形象与音色匹配、工作区重置和本地草稿管理。
- 增加文案及音色试听，并展示真实或预估说话时长。
- 优化官方数字人视频和封面素材，去除水印并完善不同场景的推荐文案。
- 完成数字人新任务从 Seedance 向 KIE 可灵数字人的最终切换。
- 保留历史 Seedance 任务的查询兼容，避免存量任务中断。
- 增加 KIE 文件上传链路，头像、肖像和音频可统一上传至供应商。
- Ark 未配置时支持个人形象使用本地资源模式，降低形象上传对 Ark 的依赖。
- 将数字人音频上限统一为 15 秒，并补充音色 ID 到 MiniMax 实际音色 ID 的映射。
- 输出数字人前后端 Diff 与后端对接说明文档。

### 2. 爆款图文工作台升级

- 重构爆款图文生成页面和整体交互布局。
- 新增图片配置面板、生成结果面板和历史记录样式。
- 新增小红书笔记预览、手机预览和风格模板预览。
- 完善文章配图配置、文案结果展示和重新生成相关交互。
- 将原有全局样式迁移为 SCSS，并拆分文章模块样式，降低单文件维护成本。

### 3. 全站积分计费统一

- 新增统一计费规则和计费报价逻辑。
- 接入图片、视频、数字人、文章、语音、音乐、转写、去水印、画质增强、抠图和提示词反推等模块。
- 前端新增统一积分展示组件和计费规则接口。
- 统一任务创建前扣费、失败退款和余额更新流程。
- 修复部分页面积分展示不准确的问题。

### 4. 模型与生成基础设施

- 图片、视频和数字人生成阶段性接入 Seedance，并完善模型配置和任务调用。
- 数字人最终调整为 KIE 可灵生成方案。
- 增加统一 ffmpeg 路径解析：
  - 优先使用 `FFMPEG_PATH`。
  - 本地环境可使用 `@ffmpeg-installer/ffmpeg`。
  - 部署环境可回退到系统 `ffmpeg`。
- 恢复后端 ffmpeg 运行时依赖，保证数字人抽帧和音频时长检测能力。

### 5. 账户与个人中心

- 登录、注册和找回密码页面调整为浅色视觉风格。
- 登录流程增加用户协议和隐私政策勾选。
- 新增用户协议和隐私政策独立页面。
- 新注册用户从 25 个公共头像中随机分配默认头像。
- 将个人中心、我的收藏和我的资产拆分为独立导航入口。
- 个人中心新增账号设置弹窗。
- 我的资产中生成失败的卡片禁用收藏操作，避免无效状态进入收藏。

### 6. 创作中心与通用体验

- 替换创作中心顶部 Banner 图片并优化展示。
- 优化大模型、图片生成和视频生成页面的输入框样式。
- 调整数字人灵感素材的封面、视频和场景描述。
- 优化生成结果素材在预览、下载和资产入口之间的跳转体验。

## 三、关键技术调整

### 数字人最终链路

```text
文本驱动：
文案 -> MiniMax TTS -> KIE 文件上传 -> KIE 可灵数字人 -> 任务轮询 -> 视频结果

音频驱动：
用户音频 -> 时长校验 -> KIE 文件上传 -> KIE 可灵数字人 -> 任务轮询 -> 视频结果
```

### 数字人任务兼容

- 新任务使用 KIE 可灵数字人。
- 历史 Seedance 任务继续调用 Ark 查询接口。
- 任务失败或结果缺失时执行积分退款。
- 两类数字人任务统一向历史页面提供状态、进度、结果地址、缩略图和错误信息。

### 统一计费规则

- 视频类能力按生成时长计算积分。
- 文本驱动数字人同时计算视频和 AI 配音费用。
- 上传音频驱动只计算数字人视频费用。
- 计费逻辑集中在后端，前端通过统一组件展示预估积分。

## 四、当前问题与风险

1. 数字人文案输入框失焦会自动请求 TTS 试听，可能增加 MiniMax 调用次数，需要增加缓存、限流或调整触发方式。
2. 文案和音色快速变化时，旧试听请求可能覆盖新状态，需要增加请求序号或取消机制。
3. 个人形象上传 KIE 时部分路径固定使用 `image/jpeg`，PNG/WebP 需要按真实 MIME 上传。
4. 数字人页面的视频规格、照片数字人的分辨率和画面比例暂未传入后端，当前主要由环境变量控制。
5. AI 创建数字人形象目前仅有前端表单和模拟进度，尚未接入真实后端任务。
6. 数字人任务进度目前为阶段性固定值，尚未透传供应商真实进度。
7. 照片数字人路由允许 20 MB 文件，但 service 对肖像限制为 10 MB，需要统一。
8. 轮询接口异常时当前记录任务错误，但最终重试和退款策略仍需明确。
9. 今日提交中包含部分临时检查文件和较大的媒体资源，后续应继续清理仓库中的生成产物。

## 五、验证情况

- 数字人相关前端生产构建通过。
- 数字人后端核心变更文件通过 `node --check`。
- 已完成数字人前后端接口和字段契约梳理。
- 前端构建仍存在大体积 Chunk 提示，后续可按功能模块进行动态加载。
- 乱码检查命中已有检查脚本自身的检测字符，不属于本次核心业务改动。

## 六、明日计划

1. 配合后端完成数字人 KIE 请求参数、模型配置和结果解析联调。
2. 修复数字人试听请求竞态，并减少非主动触发的 TTS 请求。
3. 补齐个人形象上传的真实 MIME 处理。
4. 明确分辨率、比例和视频规格由前端还是后端控制，并统一接口契约。
5. 验证 KIE 任务创建、轮询、失败退款、历史 Seedance 兼容和重新生成流程。
6. 完善数字人任务进度、错误提示和文件清理策略。
7. 继续回归爆款图文、个人中心、收藏和资产页面。
8. 清理临时文件并评估前端代码拆包，降低主包体积。

## 七、主要提交

| Commit | 内容 |
| --- | --- |
| `302cf181` | 优化大模型、图片生成和视频生成输入框 |
| `74ca59b7` | 替换创作中心顶部图片 |
| `15806567` | 视频和数字人阶段性切换到 Seedance |
| `52fd30fc` | 全站生成能力统一积分计费 |
| `d90ca8b3` | 优化数字人视频、封面和提示词 |
| `5e0493be` | 恢复 ffmpeg 运行时依赖 |
| `0c333218` | 登录注册浅色化及协议、隐私政策 |
| `e4bfb318` | 新用户随机分配公共头像 |
| `f36e8b3c` | 修复积分计费显示 |
| `927fc38e` | 拆分个人中心、收藏和资产入口 |
| `f2fe7235` | 失败资产禁用收藏 |
| `75728408` | 重构爆款图文并新增数字人 V2 UI |
| `2b2060dc` | 修复数字人生成流程并新增照片数字人、历史页 |
| `6cb78569` | 完善数字人生成工作流和音频驱动 |
| `eedc7d85` | 数字人最终切换至 KIE |
| `5b8a41a7` | 补充数字人后端对接文档 |
