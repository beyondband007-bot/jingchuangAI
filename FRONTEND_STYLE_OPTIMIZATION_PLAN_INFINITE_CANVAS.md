# 前端样式优化方案（含无限画布一致性专项）

> 版本：V5 草案  
> 日期：2026-07-27  
> 范围：`frontend-app` 主站与 `frontend-app/canvas-app` 无限画布子应用  
> 目标：在不改变核心工作流能力的前提下，建立统一、克制、可维护的 Facemini 产品体验。

---

## 1. 背景与结论

主站已经形成以紫色品牌色、浅色工作台、圆角面板和 64px 顶栏为主的视觉基线；无限画布虽然已接入主站导航，但仍是独立的 Vue + Tailwind + Naive UI 应用，并通过 iframe 运行。它沿用绿色主色、Slate 灰阶、独立暗色主题和另一套组件参数，用户在进入画布后会明显感到产品“换了一套皮肤”。

本轮优化不应把画布简单改成营销页风格。画布需要更高的信息密度、更弱的装饰、更明确的操作层级；应当以主站的品牌令牌、文字体系、控件状态和反馈方式为基础，形成“同一品牌下的专业创作工作台”。

### 本轮必须达成的结果

1. 画布的颜色、字体、圆角、阴影、焦点、动效和状态色与主站共用同一套语义定义。
2. 从主站进入画布时，顶栏、项目名、资产入口、积分/登录状态和返回逻辑连续一致，不出现重复或矛盾的导航。
3. 画布中“主操作、次操作、工具操作、危险操作”的层级一眼可辨，避免所有按钮使用同一绿色强调色。
4. 节点、连线、浮层、输入框、对话框、加载/错误/空状态拥有统一的视觉和交互规则。
5. 在 375、768、1024、1440、1920 五档宽度下可用；小屏不强行复刻桌面画布，而是提供明确的降级布局。
6. 样式不再散落于 JSX/Vue 内联对象、Tailwind 原子类和全局覆盖之间；后续变更可定位、可回归、可审计。

### 非目标

- 不在本轮重写 Vue Flow 节点编排、保存、任务或模型调用逻辑。
- 不改变现有主站品牌主色与其他功能页面的产品文案。
- 不为了“技术统一”而立即把画布从 Vue 重写为 React；先统一设计契约和体验层。
- 不新增没有设计用途的 `override.css`、`final.css`、`temp.css` 一类补丁文件。

---

## 2. 现状审计与问题清单

| 维度 | 主站基线 | 无限画布现状 | 用户影响 | 优先级 |
| --- | --- | --- | --- | --- |
| 技术边界 | React、CSS、主站令牌 | Vue、Tailwind、Naive UI、iframe | 样式隔离，无法自然继承主站主题 | P0 |
| 品牌主色 | `--brand-primary: #5a2cfc`，紫色渐变 | `--accent-color: #22c55e`，绿色 | 进入画布即切换产品品牌感 | P0 |
| 基础灰阶 | 紫调浅灰与语义 Surface | Slate 灰阶（`#f8fafc` / `#1e293b`） | 面板、边框、文字色不一致 | P0 |
| 字体与字号 | 主站令牌有 12/13/14/16/18/20/32px 规范 | 使用系统字体和大量 Tailwind 任意组合 | 文字密度、标题层级不稳定 | P1 |
| 圆角/阴影 | 6/8/12/16/24px 与语义阴影 | 画布中普遍 `rounded-xl`、`shadow-lg` | 控件显得更“组件库默认风格” | P1 |
| 顶栏与导航 | 主站提供 64px 顶栏、左侧导航与资产入口 | iframe 内再次渲染 `AppHeader`、返回和“我的资产” | 导航重复，信息架构不连续 | P0 |
| 登录门禁 | React Host 有一份门禁 | Vue `App.vue` 再有一份近似门禁 | 文案、渐变、按钮与状态可能漂移 | P1 |
| 主题策略 | 当前主站以浅色工作台为主 | 画布有独立 `.dark` 与主题切换 | 用户切换后主站/画布不同步 | P1 |
| 反馈状态 | 已有统一空态/错误态方向 | 保存、生成、拖入、下载、冲突各自呈现 | 任务结果难以扫读，错误恢复路径弱 | P1 |
| 响应式 | 有五档宽度基线 | 画布仅以 `isMobile` 局部隐藏 MiniMap | 手机端工具、输入、节点易拥挤或遮挡 | P0 |

### 已识别的代码证据

- 主站令牌位于 `frontend-app/src/styles/tokens.css`，已定义品牌、语义色、字体、间距、圆角、层级和动效变量。
- 画布令牌位于 `frontend-app/canvas-app/src/style.css`，单独定义 `--bg-*`、`--text-*`、`--accent-*`；默认强调色为绿色。
- `InfiniteCanvasHost.jsx` 用内联对象定义 iframe、加载与登录门禁；`canvas-app/src/App.vue` 又定义了一套相近门禁，存在重复来源。
- `Canvas.vue` 同时承载顶栏、画布、浮动工具栏、右键菜单、缩放、底部输入、对话框和工作流面板，视觉职责密集。
- 主站在无限画布路由下已隐藏内容区滚动；画布应把 iframe 内部视口作为唯一滚动/拖拽区域，避免双层滚动。

---

## 3. 目标体验与设计原则

### 3.1 产品体验定位

画布是“沉浸式创作工作台”，不是普通表单页。页面应只保留三类持续可见的信息：

1. **全局上下文**：项目名称、保存状态、账户/资产入口、返回主站。
2. **创作上下文**：画布节点、连接关系、选中对象和当前任务状态。
3. **即时行动**：添加节点、撤销/重做、缩放、运行/输入提示词。

其余功能通过上下文菜单、抽屉或面板按需出现，避免浮动工具同时抢占画布中心。

### 3.2 统一原则

- **共享语义，不复制色值**：画布使用 `--canvas-*` 映射到主站的语义令牌；业务组件不得直接散写 `#5a2cfc`、`#22c55e` 等颜色。
- **紫色表达产品行动，颜色表达节点类型**：主按钮、选中态和焦点使用品牌紫；文本/图片/视频/模型等节点可保留辅助识别色，但只用于节点图标、连接点和小型状态标记。
- **画布优先，面板次之**：默认背景低对比，浮层白色且有边界；避免大面积渐变、重阴影或营销插画干扰节点阅读。
- **固定结构，弹性内容**：顶栏高度、工具触点和浮层留白稳定；项目名、任务文案、节点内容允许截断、换行或滚动，不能挤压主要操作。
- **状态可恢复**：保存失败、权限不足、任务失败必须给出原因、下一步动作和保留当前画布的承诺。
- **可访问性内建**：键盘焦点、快捷键提示、对比度、减少动态效果和触屏目标尺寸不能最后补救。

---

## 4. 设计系统对齐方案

### 4.1 令牌桥接层

在 `canvas-app/src/styles/` 新增唯一的画布设计入口，例如：

```text
styles/
  canvasTokens.css       # 仅定义/映射令牌
  canvasBase.css         # reset、字体、focus、reduced motion
  canvasShell.css        # 顶栏、画布容器、工具区
  canvasControls.css     # 按钮、菜单、输入、状态条
  canvasNodes.css        # 节点、连接点、选中/执行状态
  canvasResponsive.css   # 仅五档标准断点
  index.css              # 只负责以上文件的导入顺序
```

`main.js` 只导入 `styles/index.css`；旧 `style.css` 的内容迁移后删除或仅保留过渡性导入，避免双入口。

建议的桥接令牌（数值以主站 `tokens.css` 为唯一事实来源）：

| 画布语义 | 映射目标 | 用途 |
| --- | --- | --- |
| `--canvas-bg` | `--app-surface-muted` | 画布基础背景 |
| `--canvas-grid` | `--app-border` 的低透明度版本 | 网格与辅助线 |
| `--canvas-surface` | `--surface-panel` | 工具栏、菜单、输入面板 |
| `--canvas-border` | `--app-border` | 常规边框 |
| `--canvas-text` | `--text-primary` | 主文字 |
| `--canvas-text-muted` | `--text-muted` | 次级信息 |
| `--canvas-action` | `--brand-primary` | 主操作、选中、焦点 |
| `--canvas-action-hover` | `--brand-primary-strong` | 主操作悬停 |
| `--canvas-focus` | `--brand-focus-ring` | 焦点环 |
| `--canvas-radius-control` | `--radius-control` | 小控件 |
| `--canvas-radius-panel` | `--radius-lg` | 面板/菜单 |
| `--canvas-shadow-panel` | `--shadow-panel` | 可浮起面板 |

由于 iframe 不能直接继承父页面 CSS 变量，推荐采用构建期共享方式：将主站令牌抽到仓库根部的可复用 CSS 文件（例如 `frontend-app/shared/design-tokens.css`），主站与 canvas 各自导入。若暂不调整目录，则建立脚本校验两份令牌的关键值一致，禁止手工复制后继续漂移。

### 4.2 色彩规则

| 场景 | 规范 | 禁止 |
| --- | --- | --- |
| 主操作（添加、运行、登录、确认） | 品牌紫实色或既有紫色渐变；白色文字 | 使用绿色作为默认品牌按钮 |
| 次操作（取消、缩放、更多） | 白底/浅紫悬停、细边框 | 与主操作同等实心强调 |
| 当前选中节点/工具 | 浅紫填充 + 紫色描边/焦点环 | 仅靠颜色、无形状或文字区别 |
| 节点类型 | 蓝/紫/琥珀/绿等低饱和辅助色，固定映射 | 使用红/绿表达普通类型，造成状态误读 |
| 成功/警告/失败 | 仅用 `--success` / `--warning` / `--danger` 及 soft/border 配色 | 将品牌紫当成功提示或将绿色混作成功和主操作 |
| 网格与画布背景 | 低对比紫灰/中性灰，内容优先 | 纯白大背景叠加强网格造成视觉噪声 |

### 4.3 排版与空间规则

- 顶栏、工具栏、菜单正文采用 14px；辅助文字与快捷键采用 12px；项目名和关键面板标题采用 16px；不在画布内使用大于 20px 的标题。
- 常规控件高 34px；图标按钮 34px 或 40px；触屏模式最小点击区 44px。
- 节点内边距 12px，节点间最小逻辑间距 16px；工具组间隔 8px；浮层与屏幕边缘默认 16px。
- 圆角只使用 8px（控制）、12px（节点/菜单）、16px（大面板/弹窗）；不使用随意的 `rounded-xl/2xl` 替代设计语义。
- 阴影只用于脱离画布的层；节点默认只用边框，选中时使用品牌焦点环而不是加重阴影。

---

## 5. 无限画布的具体改造设计

### 5.1 导航与顶部结构（P0）

当前主站已有全局侧栏和顶栏语境，iframe 内 `Canvas.vue` 仍渲染完整 `AppHeader`。改造前先由产品确认采用以下模式之一；推荐模式 A。

| 模式 | 方案 | 适用情况 | 建议 |
| --- | --- | --- | --- |
| A：画布专用工作台顶栏 | 保留 iframe 内顶栏，但视觉和信息与主站完全对齐；主站顶栏在画布路由下隐藏或仅保留侧栏 | 画布需要项目保存、快捷操作和沉浸空间 | 推荐 |
| B：主站顶栏托管 | iframe 不渲染顶栏，通过 `postMessage` 同步项目名、保存状态和动作 | 追求单一外壳，愿意增加跨应用通信 | 后续演进 |

模式 A 的顶栏布局：

```text
返回 / 面包屑  |  项目名称（可重命名） · 保存状态  |  资产  下载  帮助  账户
```

- 高度统一为 64px，白色背景、1px 底边框，不使用默认 Tailwind 大 padding。
- “已保存/保存中/保存失败/冲突”使用图标 + 文字；失败或冲突为可点击状态，打开明确处理面板。
- “我的资产”使用主站一致的图标、文字和入口名称；点击仍通过既有 `postMessage` 跳转主站资产模块。
- 删除、项目级设置只放入项目名下拉菜单；下载为有可下载内容时才高亮的次操作。
- 返回按钮应返回“无限画布项目列表”或上一次主站位置，不能在 iframe 内和浏览器历史产生不一致行为。

### 5.2 画布主体与浮动控件（P0）

1. **画布背景**：使用低对比 `--canvas-bg`；网格默认可见但弱化，缩放较低时自动降低对比度；提供“显示网格”选项而非固定强显示。
2. **左侧工具栏**：按“创建 / 编辑 / 历史”分组。添加节点为唯一主按钮；模板、选择、撤销、重做为次按钮。禁用按钮需要 tooltip 解释，例如“暂无可撤销操作”。
3. **节点菜单与右键菜单**：统一为同一菜单组件，宽度、图标列、热键列、悬停/焦点/分隔线规则一致。菜单必须在视口边缘自动翻转，不能在右下角被截断。
4. **缩放控制**：固定在右下角，与 MiniMap 互不遮挡；显示缩放百分比，提供“适应画布”按钮和键盘快捷键提示。
5. **底部创作输入**：将当前大浮层降为“创作指令条”。无任务时高度紧凑；输入聚焦或有生成进度时向上展开。推荐标签只在空画布或输入聚焦后显示，避免持续占用画布。
6. **拖入反馈**：拖入时使用品牌紫虚线边框、半透明遮罩和明确文案；不使用全屏高饱和遮罩。多文件拖入应显示文件数量。

建议的桌面层级：

```text
z=0    画布 / 网格 / 连线
z=10   节点
z=20   选中节点、连接预览
z=100  左工具栏、缩放、MiniMap、指令条
z=200  节点菜单、右键菜单、提示卡
z=400  工作流抽屉
z=1200 模态确认框
z=1600 Toast / 严重错误提示
```

### 5.3 节点系统（P1）

统一每个节点的结构：

```text
节点类型图标 + 类型名称            更多操作
节点标题 / 主要输入
配置或媒体预览
状态与关键元数据                   执行操作
输入/输出连接点
```

| 节点状态 | 视觉规则 | 辅助信息 |
| --- | --- | --- |
| 默认 | 白色面板、常规边框 | 类型图标与标题 |
| 悬停 | 边框加深，不位移 | 可出现“拖动/更多”提示 |
| 选中 | 紫色 2px 描边 + 焦点环 | 显示连接点和快捷操作 |
| 处理中 | 顶部细进度条或状态芯片，避免全节点闪烁 | 阶段、可取消性 |
| 成功 | 小型成功图标/时间，不用整卡绿色 | 结果预览、重新运行 |
| 失败 | 危险色状态芯片 + 错误摘要 | 重试、查看详情 |
| 失效 | 低饱和遮罩 + “上游已变更”标识 | 重新运行路径 |

- 节点色不得同时承担“类型”和“任务结果”两类语义。
- 长标题最多两行，第三行省略；媒体预览采用固定比例并支持放大查看。
- 连接点保留类型颜色，但尺寸、描边和命中区统一；仅在连接或选中时提升显著性。
- 连线默认细灰，选中为品牌紫；执行中的连线可使用低频流动效果，并受 `prefers-reduced-motion` 控制。

### 5.4 面板、弹窗与状态反馈（P1）

- 工作流模板由居中浮层改为右侧抽屉（桌面 360–420px），打开后不遮住全部画布；移动端改全屏页。
- 重命名、删除、下载沿用同一弹窗尺寸、圆角、按钮顺序。危险操作的确认按钮为红色，不能复用主品牌按钮。
- 保存失败：顶部状态变为“保存失败”，同时给出非阻塞 Toast；点击展开失败原因、最后保存时间、重试按钮和“导出本地备份”。
- 生成中：在对应节点显示局部反馈；全局只显示轻量任务计数，不用整页加载遮罩。
- 空画布：首屏提供一个简短主引导（“添加节点”/“从模板开始”）和少量示例，而不是与主站首页重复的营销式 Hero。

### 5.5 登录、账户与跨应用通信（P1）

登录门禁只保留一个视觉源。推荐由 React `InfiniteCanvasHost` 负责未登录和加载态，Vue 子应用仅在 iframe 内凭证失效时发送 `auth-required` 消息并显示简短恢复状态。

| 事件 | 发送方 | 处理方 | UI 结果 |
| --- | --- | --- | --- |
| `auth-required` | 画布 | 主站 | 打开主站登录弹窗，画布保留当前状态 |
| `open-assets` | 画布 | 主站 | 切换到资产页 |
| `credits-changed` | 画布 | 主站 | 更新统一积分显示 |
| `project-state`（新增） | 画布 | 主站 | 可选：同步项目名、保存/错误状态 |
| `theme-changed`（新增） | 主站 | 画布 | 仅在暗色主题正式纳入主站后使用 |

消息必须校验 `origin`、`source` 和明确的 payload schema；不要以 CSS 或 DOM 跨 iframe 修改界面。

---

## 6. 响应式与可访问性策略

### 6.1 标准断点

只使用项目现有标准：375 / 768 / 1024 / 1440 / 1920px。禁止为单个元素新增 600、900、1200 等临时断点。

| 宽度 | 体验策略 |
| --- | --- |
| 1440 / 1920 | 完整工作台：左工具栏、右下 MiniMap、底部指令条、桌面抽屉 |
| 1024 | 保留工作台；工具栏收紧，项目标题可截断，指令条最大宽度缩小 |
| 768 | 工具栏变为左下横向浮条；隐藏 MiniMap；工作流使用全高抽屉 |
| 375 | 默认进入“移动创作模式”：画布可平移缩放，节点编辑、模板、项目操作以底部抽屉/全屏面板进行；保留添加、撤销、适应视图三个核心操作 |

### 6.2 键盘、焦点与动效

- 所有图标按钮必须有可读名称（`aria-label` 或 tooltip），工具栏可通过 Tab 到达。
- 焦点环使用统一 `--canvas-focus`，不能被 `outline: none` 直接移除。
- 快捷键至少覆盖：添加节点、撤销、重做、适应视图、缩放、删除选择、打开命令/节点菜单；在帮助入口中可发现。
- `prefers-reduced-motion: reduce` 下关闭连线流动、节点弹跳、面板位移动画和持续 pulse，加载改为静态进度/文本。
- 文本与背景对比度达到 WCAG AA；不能只通过红/绿区分成功与失败。

---

## 7. 实施路线图

### 阶段 0：建立基线（0.5–1 天）

- 截图并记录主站工作台、画布首页、空画布、有节点、选中节点、生成中、失败、项目菜单、工作流面板和移动端状态。
- 确认导航采用“画布专用顶栏”还是“主站顶栏托管”。未确认前不改顶栏 DOM。
- 建立画布页面状态矩阵与 Playwright 路由/截图用例目录。

**完成标准**：视觉基线可重复，存在问题可逐项对比，不依赖主观记忆。

### 阶段 1：统一设计令牌与基础控件（1–2 天，P0）

- 建立共享令牌或受校验的桥接令牌，替换画布的 `--accent-color`、Slate 灰阶和独立圆角/阴影定义。
- 迁移画布基础 CSS、字体、焦点和减少动效规则至单一样式入口。
- 统一 Naive UI theme overrides：按钮、输入、卡片、Dialog、Dropdown、Switch 的圆角、尺寸和主色。
- 将 `InfiniteCanvasHost` 内联样式提取为 `infiniteCanvasHost.css`，登录门禁复用主站反馈组件或共享样式。

**完成标准**：绿主色不再作为画布默认品牌色；主站/画布关键令牌可以一一对照；无内联大块样式对象。

### 阶段 2：重构工作台外壳（1–2 天，P0）

- 按确认的导航模式统一顶栏，解决重复返回、资产入口与账户状态。
- 固化画布、工具栏、底部指令条、缩放控件和 MiniMap 的层级与避让规则。
- 合并节点菜单和右键菜单的视觉组件，补齐视口翻转。

**完成标准**：桌面 1024/1440 无控件重叠、无遮挡、无双滚动；用户能在 3 秒内定位项目名、保存状态、添加节点和资产入口。

### 阶段 3：节点与任务反馈（2–3 天，P1）

- 为所有节点补齐 default/hover/selected/processing/success/error/stale 状态。
- 将生成进度从底部全局提示迁移为节点局部反馈；保留轻量全局任务摘要。
- 统一抽屉、确认弹窗、Toast、下载和保存冲突的状态语言。

**完成标准**：任何节点发生任务变化时，用户无需阅读日志即可知道“正在做什么、是否成功、下一步怎么做”。

### 阶段 4：移动与可访问性（1–2 天，P0）

- 实现 768/375 的工具栏、节点编辑和工作流面板降级方式。
- 补齐键盘焦点、快捷键帮助、最小触控尺寸、对比度和减少动效。

**完成标准**：五档视口无横向溢出；核心新建/编辑/保存/查看资产链路可用；键盘焦点可见。

### 阶段 5：治理与长期维护（持续，P2）

- 将画布纳入主站样式检查、断点检查与视觉回归。
- 为令牌、主题覆盖和跨应用消息定义 ownership，指定修改入口。
- 定期清理 Tailwind 任意值和重复的 Naive UI 内联样式，不能以“暂时可用”为理由增加新覆盖层。

---

## 8. 验收矩阵与质量门禁

| 场景 | 必验内容 |
| --- | --- |
| 首次进入画布 | 主站到 iframe 无白屏闪烁；顶栏和品牌连续；登录态正确 |
| 未登录/会话失效 | 仅一个门禁视觉；登录后回到原项目，不丢失上下文 |
| 空画布 | 首要动作清晰；模板、添加节点、输入创作均可发现 |
| 有节点画布 | 选择、连线、拖动、缩放、适应视图、撤销/重做可用 |
| 任务运行 | 节点局部反馈、全局摘要、取消/重试路径正确；减少动效有效 |
| 保存失败/冲突 | 状态明显、原因可看、可重试、可导出备份 |
| 资产/积分 | 通过跨应用消息返回主站，状态即时更新 |
| 弹窗/菜单 | 键盘可操作、焦点不丢失、边缘不裁切、危险操作有确认 |
| 响应式 | 375/768/1024/1440/1920 无水平溢出、浮层不遮挡主要操作 |
| 深色模式 | 若未正式支持，应隐藏画布独立切换；若支持，主站和画布同步切换并全量验证 |

建议新增/扩展以下自动化检查：

```powershell
cd E:\CodeX\AI工作台\frontend-app
npm.cmd run check:styles
npm.cmd run check:breakpoints
npm.cmd run check:focus-visible
npm.cmd run check:reduced-motion
npm.cmd run build
npx.cmd playwright test

cd E:\CodeX\AI工作台\frontend-app\canvas-app
npm.cmd run build
```

新增专用检查：

- `check:canvas-tokens`：校验桥接令牌与主站关键令牌一致，禁止画布出现绿色默认主色。
- `check:canvas-style-entrypoints`：保证画布只有一个样式入口，不新增全局补丁文件。
- `check:canvas-a11y`：检查图标按钮名称、焦点可见性、最小触控区域和减少动效。
- Playwright 截图：覆盖空/选中/生成/失败/菜单/移动端等状态，并在 iframe 内定位。

---

## 9. 风险、依赖与决策点

| 风险或依赖 | 处理方式 |
| --- | --- |
| iframe 无法直接继承主站 CSS | 使用共享令牌文件或构建时同步，不依赖运行时 CSS 穿透 |
| 双应用组件库不同 | 统一设计 token 和组件行为，不强制短期统一框架 |
| 顶栏是否保留涉及信息架构 | 阶段 0 先确认模式 A/B，避免返工 |
| 深色模式仅画布支持 | 在主站没有完整暗色策略前，先移除/隐藏画布单独开关；不要让用户进入不一致主题 |
| 画布功能密度高 | 分阶段替换样式，节点执行/保存逻辑不与视觉重构同批大改 |
| 视觉回归覆盖 iframe | 测试中显式等待 iframe 加载并定位 frame，不能只截主站外壳 |

---

## 10. 文件级落地清单

| 文件/目录 | 建议动作 |
| --- | --- |
| `frontend-app/src/styles/tokens.css` | 作为主站令牌事实来源；必要时抽取为共享文件 |
| `frontend-app/src/features/infinite-canvas/InfiniteCanvasHost.jsx` | 移除内联样式，保留鉴权和消息桥接职责 |
| `frontend-app/src/features/infinite-canvas/` | 新增 Host 专属 CSS 与加载/错误/门禁状态组件 |
| `frontend-app/canvas-app/src/style.css` | 拆分为画布唯一入口及职责明确的样式文件；移除绿色默认品牌令牌 |
| `frontend-app/canvas-app/src/App.vue` | 取消与 Host 重复的门禁视觉；统一主题与 Naive UI provider |
| `frontend-app/canvas-app/src/components/AppHeader.vue` | 按确认的工作台顶栏方案重构，支持保存状态、项目操作和主站入口 |
| `frontend-app/canvas-app/src/views/Canvas.vue` | 按 shell / toolbar / menus / composer / dialogs 分离视觉结构；不混入全局 CSS |
| `frontend-app/canvas-app/src/components/nodes/` | 建立共享 Node Shell 与状态类，逐个迁移节点 |
| `frontend-app/canvas-app/tailwind.config.js` | 不再维护独立绿色 `primary` 色板；只引用画布语义令牌或与主站一致的色板 |

---

## 11. 完成定义

本方案完成不以“颜色从绿色改成紫色”作为判断，而以以下结果为准：

1. 用户从主站进入无限画布，不会感觉进入另一个产品。
2. 画布仍保留专业工作台的高密度与沉浸感，且主/次操作、节点状态和任务反馈更清楚。
3. 双应用之间有明确的令牌、消息和导航契约，样式不再靠复制和临时覆盖维持。
4. 五档响应式、焦点、减少动效、错误恢复和 iframe 场景均有自动化回归。
5. 新功能能沿用既定入口和令牌完成样式，而无需继续添加独立主题或补丁文件。
