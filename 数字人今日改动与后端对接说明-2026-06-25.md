# 数字人今日改动与后端对接说明

> 日期：2026-06-25  
> 对比基线：`b32049bf`（2026-06-24 22:39:21）  
> 当前版本：`eedc7d85`（2026-06-25 23:28:32）  
> 范围：只统计数字人形象、照片数字人、数字人历史记录及其直接依赖的后端能力，不包含文章、登录、通用资产页等无关改动。

## 1. 今日数字人改动概览

今天的数字人改动经历了以下阶段：

1. 数字人视频生成从原有 KIE 方案阶段性切换到 Seedance。
2. 数字人和照片数字人接入统一积分计费。
3. 优化官方数字人形象视频、封面和生成提示词。
4. 新增数字人 V2 工作台、照片数字人工作台和统一历史记录页面。
5. 增加照片数字人的音频驱动能力。
6. 最终将数字人形象和照片数字人的新任务统一切回 KIE 可灵数字人。
7. 保留对历史 Seedance 任务的轮询兼容。

最终状态不是上午的 Seedance 方案，而是：

- 数字人形象：MiniMax TTS/上传音频 + KIE 文件上传 + KIE 可灵数字人。
- 照片数字人：MiniMax TTS/上传音频 + KIE 文件上传 + KIE 可灵数字人。
- 历史 Seedance 任务：继续按 Ark/Seedance 接口查询，避免存量任务失效。

数字人专属净变化共涉及 32 个文件，约新增 7,622 行、删除 116 行。

## 2. 最终业务结构

前端现在有三个数字人入口：

| 入口 | 前端模块 | 后端模块 |
| --- | --- | --- |
| 数字人形象 | `frontend-app/src/features/digital-human-v2` | `backend/src/modules/digital-human` |
| 照片数字人 | `frontend-app/src/features/photo-digital-human-v2` | `backend/src/modules/image-digital-human` |
| 历史记录 | `frontend-app/src/features/digital-human-hub` | 同时读取上述两个后端模块 |

数字人形象和照片数字人虽然共用音色、积分和 KIE 供应商，但仍是两套任务表、两组任务接口。

## 3. 前端改动

### 3.1 数字人 V2 工作台

新增完整的数字人 V2 页面，主要能力如下：

- 官方形象和我的形象切换。
- 官方数字人形象预览和选择。
- 我的形象列表合并展示：
  - 用户上传形象。
  - 数字人形象历史任务。
  - 照片数字人历史任务。
- 文案输入、选中文案 AI 优化。
- 音色选择、语速和情绪设置。
- 音色试听和整段文案试听。
- 视频规格选择。
- 生成中、成功、失败预览状态。
- 删除任务和重新生成。
- 保存、恢复和删除本地草稿。
- 跳转“我的资产”。
- 重置当前工作区。

数字人形象创建请求：

```json
{
  "avatarId": "public-anchor-dialogue",
  "avatarName": "主播对话",
  "driveMode": "text",
  "text": "口播文案",
  "performance": "",
  "voiceId": "female-qn-qingse",
  "model": "kling-ai-avatar-pro",
  "speed": 1,
  "volume": 1,
  "pitch": 0,
  "emotion": ""
}
```

注意：

- 当前 V2 页面固定使用 `driveMode: "text"`。
- 后端仍保留数字人形象的上传音频接口和 `driveMode: "audio"` 能力。
- 页面上的 `videoSpec` 当前仅用于前端显示和草稿，未传给创建任务接口。
- `performance` 已传给后端，但当前后端未消费。

### 3.2 文案与音色试听

数字人 V2 增加整段文案试听：

```http
POST /api/digital-human/voices/preview
Content-Type: application/json
```

```json
{
  "text": "需要试听的文案",
  "voiceId": "female-qn-qingse",
  "speed": 1,
  "emotion": ""
}
```

前端依赖返回：

```json
{
  "voiceId": "female-qn-qingse",
  "audioBase64": "...",
  "audioDataUrl": "data:audio/mpeg;base64,...",
  "durationMs": 8500,
  "maxDurationMs": 15000,
  "isTooLong": false,
  "videoDuration": 9
}
```

当前交互还会在文案输入框失焦时自动请求试听接口，用于获取准确时长。该行为可能增加 MiniMax 调用次数，后端最好做内容级缓存或限流。

### 3.3 音色可用范围

前端当前只允许以下音色进入数字人生成流程：

```text
female-qn-qingse
moss_audio_ce44fc67-7ce3-11f0-8de5-96e35d26fb85
moss_audio_aaa1346a-7ce7-11f0-8e61-2e6e3c7ee85d
```

其他音色仍可由 `/voices` 返回，但在前端显示为不可用。

后端音色数据新增 `providerVoiceId`，用于把产品内部音色 ID 映射到 MiniMax 实际音色 ID。后端生成和试听时都应使用 `providerVoiceId`，数据库中仍保存产品内部 `voiceId`。

### 3.4 我的形象和创建形象

上传形象调用：

```http
POST /api/digital-human/avatars
Content-Type: multipart/form-data
```

字段：

| 字段 | 类型 | 说明 |
| --- | --- | --- |
| `name` | string | 形象名称 |
| `avatar` | file | 图片文件，路由限制 30 MB |

AI 创建形象目前只有前端表单和模拟进度弹窗，没有调用后端，也不会生成真实形象。若要上线，需要后端新增独立的 AI 形象任务接口和轮询接口。

建议接口：

```http
POST /api/digital-human/avatars/ai-tasks
GET  /api/digital-human/avatars/ai-tasks/:id
```

建议创建参数：

```json
{
  "prompt": "人物描述",
  "gender": "female",
  "age": "young",
  "style": "realistic",
  "ratio": "9:16"
}
```

### 3.5 照片数字人 V2

新增独立的照片数字人工作台：

- 上传人物正面图。
- 文本驱动和音频驱动。
- 音色和语速设置。
- 模型、分辨率、画面比例选择。
- 生成任务状态与结果预览。
- 积分余额和预计消耗展示。

创建接口改为同时支持肖像和音频：

```http
POST /api/image-digital-human/tasks
Content-Type: multipart/form-data
```

字段：

| 字段 | 类型 | 必填 | 说明 |
| --- | --- | --- | --- |
| `portrait` | file | 是 | 人物肖像图片 |
| `audio` | file | 否 | 用户上传音频 |
| `text` | string | 条件必填 | 未上传音频时必填 |
| `voiceId` | string | 条件必填 | 文本驱动时使用 |
| `model` | string | 是 | 当前为 `kie-s2v-r2v` |
| `speed` | number | 否 | 默认 1 |
| `volume` | number | 否 | 默认 1 |
| `pitch` | number | 否 | 默认 0 |
| `emotion` | string | 否 | MiniMax 情绪参数 |

注意：

- 前端有分辨率和画面比例控件，但当前没有把 `resolution`、`aspectRatio` 传到后端。
- 后端当前使用环境变量中的固定分辨率。
- 肖像上传路由允许单文件最大 20 MB，但 service 又限制肖像最大 10 MB，当前限制不一致。
- 音频最大时长为 15 秒。

### 3.6 数字人统一历史记录

新增数字人历史页，同时请求：

```http
GET /api/digital-human/tasks
GET /api/image-digital-human/tasks
```

前端将两类任务归一化后合并、排序和筛选，支持：

- 全部、数字人形象、照片数字人筛选。
- 视频预览。
- 下载结果。
- 重新生成。
- 删除历史记录。

后端必须保证两类任务至少稳定返回以下公共字段：

```json
{
  "id": "123",
  "status": "processing",
  "progress": 68,
  "text": "口播文案",
  "voiceId": "female-qn-qingse",
  "voiceName": "温柔女生",
  "providerModel": "kling/ai-avatar-pro",
  "duration": 10,
  "audioDurationMs": 9500,
  "costPoints": 1210,
  "resultUrl": "",
  "thumbnailUrl": "",
  "error": "",
  "createdAt": "2026-06-25 23:00:00",
  "time": "23:00"
}
```

数字人形象任务还需要：

```json
{
  "avatarId": "public-anchor-dialogue",
  "avatarName": "主播对话",
  "driveMode": "text"
}
```

照片数字人任务还需要：

```json
{
  "portraitUrl": "/media/image-digital-human/portraits/example.png",
  "fallbackProviderModel": "kling/ai-avatar-pro",
  "usedProviderModel": "kling/ai-avatar-pro"
}
```

## 4. 后端改动

### 4.1 新任务统一切换到 KIE

数字人形象最终模型：

```text
业务 model key: kling-ai-avatar-pro
供应商 model: KIE_DIGITAL_HUMAN_MODEL
默认值: kling/ai-avatar-pro
```

照片数字人最终模型：

```text
业务 model key: kie-s2v-r2v
主模型: KIE_IMAGE_DIGITAL_HUMAN_PRIMARY_MODEL
回退模型: KIE_IMAGE_DIGITAL_HUMAN_FALLBACK_MODEL
默认值: kling/ai-avatar-pro
```

统一生成链路：

1. 校验形象、文本/音频、模型和音色。
2. 文本驱动时调用 MiniMax TTS。
3. 校验音频时长不超过 15 秒。
4. 将肖像/头像和音频上传至 KIE 文件服务。
5. 调用 KIE `/api/v1/jobs/createTask`。
6. 保存供应商任务 ID、供应商资源 URL 和音频时长。
7. 轮询 KIE `/api/v1/jobs/recordInfo`。
8. 成功后保存 `resultUrl` 和 `thumbnailUrl`。
9. 失败时标记任务失败并退还积分。

当前实现中，供应商明确返回失败或成功结果缺少 `resultUrl` 时会退款；如果仅是查询接口抛出异常，则只记录任务错误，不会自动退款。后端需要确认这是预期策略，还是应增加重试次数和最终退款机制。

KIE 可灵请求的核心输入：

```json
{
  "model": "kling/ai-avatar-pro",
  "input": {
    "image_url": "https://...",
    "reference_image": ["https://..."],
    "audio_url": "https://...",
    "prompt": "A person speaks naturally..."
  }
}
```

照片数字人的 provider 会根据模型区分参数：

- 可灵模型：`image_url`、`audio_url`、`prompt`。
- Wan speech-to-video：额外计算 `num_frames`、`frames_per_second`、`resolution` 等参数。

### 4.2 KIE 文件上传

头像/肖像和音频不再依赖 Ark 虚拟资产 URL，统一调用 KIE 文件上传服务：

```text
KIE_FILE_UPLOAD_BASE_URL/api/file-stream-upload
Authorization: Bearer KIE_API_KEY
```

上传目录：

```text
digital-human/avatar-image
digital-human/audio
image-digital-human/portrait
image-digital-human/audio
```

数字人官方形象如果是视频，后端会优先查找同目录 `posters/<name>.jpg`；没有封面时使用 ffmpeg 抽帧，再上传图片给 KIE。

### 4.3 Ark 虚拟资产降级为本地资产

当未配置 Ark OpenAPI 的 `accessKeyId` 和 `secretAccessKey` 时：

- 上传的个人形象不再调用 Ark CreateAsset。
- 后端创建 `local-*` provider asset ID。
- 资源直接标记为 `active`。
- `publicUrl` 指向本地 `/media` 资源。
- 后续数字人生成时再上传到 KIE。

此改动使“上传个人形象”不再强依赖 Ark 配置。

### 4.4 历史 Seedance 任务兼容

新任务全部走 KIE，但数据库里可能存在今天早些时候创建的 Seedance 任务。

轮询逻辑必须保留双分支：

- `model_key === "seedance-2.0"` 或 `provider_model` 为 Ark 视频模型：调用 Ark 查询接口。
- 其他任务：调用 KIE 查询接口。

数字人形象当前用 `provider_model` 是否以 `doubao-seedance` 开头判断历史 Ark 任务；照片数字人同时检查 `model_key` 和 `provider_model`。

建议后端后续增加明确的 `provider_type` 字段，避免依赖模型字符串判断供应商。

### 4.5 统一积分计费

数字人改为通过 `calculateBillingQuote` 计费：

```text
视频：120 积分/秒
AI 配音：1 积分/秒
```

文本驱动：

```text
总积分 = 视频秒数 × 120 + AI 配音秒数 × 1
```

上传音频驱动：

```text
总积分 = 音频向上取整秒数 × 120
```

创建任务时先扣积分；供应商任务创建失败、轮询失败结果或缺失结果 URL 时，调用退款逻辑。

### 4.6 音频限制

两套数字人服务现在都限制音频最长 15 秒：

```text
digital-human: 15,000 ms
image-digital-human: 15,000 ms
```

文本驱动在 MiniMax TTS 返回后校验真实音频时长。  
照片数字人的上传音频通过 ffmpeg 获取真实时长后校验。

前端文案最大长度目前为 500 字，但 500 字通常远超 15 秒，因此后端仍必须以真实 TTS 时长为最终判定依据。

### 4.7 ffmpeg 路径

数字人视频抽帧改为使用统一的 `ffmpegPath`：

1. 优先读取 `FFMPEG_PATH`。
2. 其次读取 `@ffmpeg-installer/ffmpeg`。
3. 最后使用系统命令 `ffmpeg`。

部署环境必须保证上述至少一种方式可用。

## 5. 当前 API 清单

### 5.1 数字人形象

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/digital-human/models` | 模型和默认配置 |
| GET | `/api/digital-human/avatars` | 官方形象和我的形象 |
| POST | `/api/digital-human/avatars` | 上传个人形象 |
| PUT | `/api/digital-human/avatars/:id` | 修改形象 |
| DELETE | `/api/digital-human/avatars/:id` | 删除形象 |
| GET | `/api/digital-human/voices` | 音色列表 |
| POST | `/api/digital-human/voices/design` | 设计音色 |
| POST | `/api/digital-human/voices/preview` | 音色/文案试听 |
| POST | `/api/digital-human/uploads/audio` | 上传驱动音频 |
| GET | `/api/digital-human/tasks` | 任务列表 |
| POST | `/api/digital-human/tasks` | 创建任务 |
| GET | `/api/digital-human/tasks/:id` | 查询单任务 |
| POST | `/api/digital-human/tasks/:id/regenerate` | 重新生成 |
| DELETE | `/api/digital-human/tasks/:id` | 删除任务 |

### 5.2 照片数字人

| 方法 | 路径 | 用途 |
| --- | --- | --- |
| GET | `/api/image-digital-human/models` | 模型和限制 |
| GET | `/api/image-digital-human/voices` | 音色列表 |
| POST | `/api/image-digital-human/voices/preview` | 音色试听 |
| GET | `/api/image-digital-human/tasks` | 任务列表 |
| POST | `/api/image-digital-human/tasks` | 创建文本/音频驱动任务 |
| GET | `/api/image-digital-human/tasks/:id` | 查询单任务 |
| POST | `/api/image-digital-human/tasks/:id/regenerate` | 重新生成 |
| DELETE | `/api/image-digital-human/tasks/:id` | 删除任务 |

## 6. 环境变量

后端至少需要检查：

```env
KIE_API_KEY=
KIE_API_BASE_URL=https://api.kie.ai
KIE_FILE_UPLOAD_BASE_URL=https://kieai.redpandaai.co

KIE_DIGITAL_HUMAN_MODEL=kling/ai-avatar-pro
KIE_DIGITAL_HUMAN_RESOLUTION=720p

KIE_IMAGE_DIGITAL_HUMAN_PRIMARY_MODEL=kling/ai-avatar-pro
KIE_IMAGE_DIGITAL_HUMAN_FALLBACK_MODEL=kling/ai-avatar-pro
KIE_IMAGE_DIGITAL_HUMAN_RESOLUTION=480p
KIE_IMAGE_DIGITAL_HUMAN_FALLBACK_RESOLUTION=720p

MINIMAX_API_KEY=
FFMPEG_PATH=
```

Ark 配置仅在以下场景仍需要：

- 查询历史 Seedance 任务。
- 兼容已有 Ark 虚拟资产。

新上传个人形象在 Ark 未配置时可以使用本地资产模式。

## 7. 后端需要重点修改或确认

### P0：确认 KIE 可灵接口参数

当前数字人形象 provider 发送：

```json
{
  "image_url": "...",
  "reference_image": ["..."],
  "audio_url": "...",
  "prompt": "..."
}
```

照片数字人可灵 provider 发送：

```json
{
  "image_url": "...",
  "audio_url": "...",
  "prompt": "..."
}
```

需要根据 KIE 当前模型文档确认 `reference_image` 是否需要保留，以及模型名是否确认为 `kling/ai-avatar-pro`。

### P0：统一任务供应商标识

建议数据库新增或补齐：

```text
provider_type: kie | ark
```

轮询时优先按 `provider_type` 分流，不再仅依赖 `provider_model` 字符串。

### P1：修正头像上传 MIME

数字人形象上传 KIE 时当前固定使用 `image/jpeg`。如果用户上传的是 PNG/WebP，扩展名、文件内容和 MIME 会不一致。

后端应：

- 原始图片按真实 MIME 上传。
- ffmpeg 生成的 JPG 封面才使用 `image/jpeg`。

### P1：试听接口增加缓存或限流

前端输入框失焦也会触发 MiniMax TTS。建议按以下字段生成缓存键：

```text
text + providerVoiceId + speed + volume + pitch + emotion
```

同时建议增加：

- 单用户并发限制。
- 短时间重复请求合并。
- 最大文本长度校验。

### P1：统一文件大小限制

照片数字人当前：

- multer：单文件最大 20 MB。
- service：肖像最大 10 MB。

建议统一为同一个配置，并分别明确肖像和音频限制。

### P1：明确分辨率和比例是否由前端控制

前端已提供 `resolution`、`aspectRatio` 和数字人 `videoSpec`，但没有进入任务请求。

后端和产品需要二选一：

1. 固定由环境变量控制，前端删除无效控件。
2. 接口增加 `resolution`、`aspectRatio`，后端做白名单校验后传给供应商。

### P1：实现或移除 AI 创建形象

当前 AI 创建形象是纯前端模拟，不会创建真实任务。后端如果需要支持，必须新增任务表、创建接口、轮询接口和结果入库。

### P2：任务进度改为真实进度

当前后端进度是固定映射：

```text
无 provider_task_id：24
有 provider_task_id：68
completed：100
failed：0
```

如果 KIE 返回进度字段，建议透传真实进度；否则前端只能显示阶段进度。

### P2：重新生成的文件依赖

照片数字人的 `regenerateTask` 会复用原任务的本地肖像和音频。需要确认：

- 删除任务是否会删除物理文件。
- 文件清理任务是否会提前清除仍可能被重新生成使用的文件。
- 生产环境多实例时，本地文件是否共享。

## 8. 后端验收清单

- [ ] `/api/digital-human/models` 返回 `kling-ai-avatar-pro` 且 `configured` 正确。
- [ ] `/api/image-digital-human/models` 返回 `kie-s2v-r2v`。
- [ ] 官方视频形象可以成功抽帧并上传 KIE。
- [ ] PNG、JPEG、WebP 个人形象均能正确上传。
- [ ] 文本驱动会生成 MiniMax 音频并创建 KIE 任务。
- [ ] 上传音频驱动不会重复调用 MiniMax TTS。
- [ ] 超过 15 秒的 TTS 音频和上传音频均被拒绝。
- [ ] KIE 创建失败后积分自动退还。
- [ ] KIE 轮询失败后任务错误信息可见。
- [ ] 明确轮询网络异常达到重试上限后的退款策略。
- [ ] KIE 成功结果能解析出 `resultUrl`。
- [ ] 历史 Seedance 任务仍能继续轮询并完成。
- [ ] 数字人形象和照片数字人任务均可删除、重新生成。
- [ ] 两类任务列表字段满足统一历史页展示。
- [ ] 多实例部署时头像、肖像、音频和抽帧文件可被所有实例访问。

## 9. 关键提交

| Commit | 内容 |
| --- | --- |
| `15806567` | 数字人阶段性切换到 Seedance |
| `52fd30fc` | 数字人统一积分计费 |
| `d90ca8b` | 数字人形象素材和提示词优化 |
| `75728408` | 新增数字人 V2 前端 |
| `2b2060dc` | 数字人生成流程、照片数字人和历史入口 |
| `6cb78569` | 完善数字人工作流和照片数字人音频驱动 |
| `eedc7d85` | 最终将数字人生成切换到 KIE |

## 10. 相关代码位置

后端：

```text
backend/src/modules/digital-human/
backend/src/modules/image-digital-human/
backend/src/providers/kie/digitalHuman.js
backend/src/providers/kie/imageDigitalHuman.js
backend/src/providers/kie/upload.js
backend/src/shared/billingRules.js
backend/src/shared/ffmpegPath.js
```

前端：

```text
frontend-app/src/features/digital-human-v2/
frontend-app/src/features/photo-digital-human-v2/
frontend-app/src/features/digital-human-hub/
frontend-app/src/features/image-digital-human/
frontend-app/src/api/digitalHumanApi.js
frontend-app/src/api/imageDigitalHumanApi.js
```
