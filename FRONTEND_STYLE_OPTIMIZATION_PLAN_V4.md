# 前端样式优化方案（V4：架构收敛与长期可维护性）

## 1. 方案目的与范围

本方案基于 **2026-07-25** 对 `frontend-app` 当前代码的重新审计制定。此前已完成的历史空状态统一、标准断点收敛、样式归属检查等工作不重复建设；V4 聚焦下一阶段仍有收益的结构性问题：**样式入口过多、复杂页面职责过重、样式层级与指标语义不够清晰、静态媒体交付体积偏大、视觉回归覆盖仍可体系化**。

本方案是改造清单，不直接改变产品视觉规范、接口协议或业务流程。

### 本轮目标

1. 将“一个页面导入很多样式文件”收敛为可读、可追踪的功能样式入口。
2. 拆解超大型页面组件，减少 JSX、状态逻辑和 CSS 的相互耦合。
3. 为公共样式建立清晰的层级、命名、令牌和覆盖规则，避免再次出现临时覆盖文件。
4. 将“重复选择器”统计拆分为可行动的指标，避免不同脚本的数字互相造成误解。
5. 补齐关键页面状态及五档标准宽度下的视觉回归约束。
6. 对静态视频资源建立可分阶段执行的瘦身与 CDN 迁移路径；本轮不自动删除任何被引用媒体。

### 不在本轮范围内

- 不调整品牌色、字号体系或产品文案的设计决策。
- 不修改后端 API、任务轮询、支付或鉴权逻辑。
- 不凭文件名删除图片、视频、HTML、MD 或部署文件；资源删除必须先完成引用与发布路径审计。
- 不引入 CSS-in-JS、Tailwind 等新的样式技术栈。当前项目以 CSS/SCSS 为主，先把现有体系治理清楚收益更高。

---

## 2. 当前审计基线

审计命令在 `frontend-app` 目录执行：

```powershell
npm.cmd run check:redundancy
npm.cmd run check:styles
npm.cmd run check:coverage
npm.cmd run check:breakpoints
npm.cmd run check:assets
```

| 维度 | 当前结果 | 结论 |
| --- | ---: | --- |
| 前端样式文件（CSS/SCSS） | 161 个 | 文件数已按职责收敛，并由归属检查持续保护。 |
| 前端样式总行数 | 35,343 行 | 体量可控，主要风险转为跨文件导入顺序与维护成本，而非单纯行数。 |
| 超过 800 行的样式文件 | 0 个 | 已满足现有上限，继续把 500～750 行文件按职责细化。 |
| `!important` | 0 | 保持为硬性门禁。 |
| 暗色令牌缺失/异常 | 0 | 当前检查通过。 |
| 标准断点 | 仅 375 / 768 / 1024 / 1440 / 1920 | 当前检查通过，禁止重新引入临时宽度。 |
| 真实上下文重复选择器 | 6 个 | 低于当前门禁，剩余项均已进入决策台账。 |
| `check:styles` 的原始重复选择器计数 | 421 个 | 该值与上项统计口径不同，需显式命名和说明，不能直接拿来比较。 |
| 样式源引用/归属检查 | 161 个样式源均通过 | 没有“所属模块仍导入遗留样式”的问题。 |
| 静态资源 | 1,390 个、2,091.0 MiB | 其中视频 1,745.6 MiB，属于首要交付性能风险。 |

### 2.1 当前大型样式文件

以下文件均未超出 800 行门禁，但仍混合了多个视觉职责，推荐进入分批治理：

| 文件 | 行数 | 建议拆分方向 |
| --- | ---: | --- |
| `src/features/face-swap/FaceSwapWorkbench.css` | 748 | 工作台壳层、上传区、预览/结果、历史状态、响应式。 |
| `src/features/video-workflow/videoWorkflowStates.css` | 643 | processing、result、history、empty/error 状态分离。 |
| `src/features/assets/assetsBilling.css` | 635 | 账单筛选、表格/卡片、状态反馈、移动端规则分离。 |
| `src/features/home/homeApp.css` | 603 | 首页模块、营销区块、弹层/浮层、响应式分离。 |
| `src/features/chat/chatLayoutOverrides.scss` | 599 | 识别真正覆盖规则，按 chat shell、消息区、输入区收回原模块。 |
| `src/StudioLandingShowcase.css` | 581 | 展示区块、卡片、动效及断点规则拆开。 |
| `src/features/video/videoGenStage.css` | 573 | 舞台、媒体预览、结果操作、状态层拆开。 |
| `src/features/article/articleWorkbenchBase.css` | 570 | 工作台框架、表单、结果区和窄屏布局分开。 |
| `src/styles/components.css` | 570 | 仅保留跨功能可复用组件；业务选择器必须迁出。 |
| `src/layouts/appShellFeature.css` | 562 | App Shell 基础布局与功能页补丁分离。 |

### 2.2 当前大型页面和样式导入扇出

大型页面中，样式拆分本身带来了可读性，但入口文件同时承担页面编排、状态管理和大量样式导入，顺序依赖会随功能迭代增大。

| 页面 | JSX 行数 | 样式导入数 | 主要风险 |
| --- | ---: | ---: | --- |
| `features/article/ArticleGenerationView.jsx` | 922 | 20 | 工作台主容器仅保留任务数据、请求和弹层状态；静态配置、历史网格、左侧编辑器与结果舞台均已提取。 |
| `features/assets/AssetsPage.jsx` | 1,401 | 9 | 多个资产子域和路由视图共存，布局规则容易串联。 |
| `features/image/ImageGenerationView.jsx` | 1,256 | 6 | 生成表单、结果、任务状态和弹层集中。 |
| `features/chat/ChatGenerationView.jsx` | 1,066 | 15 | 聊天壳、历史、消息流、输入区以及覆写样式并存。 |
| `features/digital-human-v2/DigitalHumanV2View.jsx` | 716 | 8 | 生成请求、轮询、上传和积分校验保留在容器；工作台展示层已提取。 |
| `features/music/MusicGenerationView.jsx` | 776 | 10 | 任务状态机保留在页面容器；创作表单与历史展示已提取为独立组件。 |
| `features/watermark/WatermarkRemovalView.jsx` | 798 | - | 表单、拖拽、处理和历史状态仍可组件化。 |
| `features/music/MusicFullPagePlayer.jsx` | 760 | - | 播放器状态和显示层可继续隔离。 |
| `features/video-workflow/VideoGenerationWorkflow.jsx` | 701 | - | 工作流步骤、状态面板及结果区域可拆分。 |

> 说明：大文件不是缺陷的充分条件。只有当它同时拥有多个业务职责、多个样式入口或无法进行局部视觉验证时，才应拆分。拆分目标是降低变更面，而不是为了压缩行数。

---

## 3. 核心问题与优先级

### P0：指标语义不一致，容易误判重复问题

`check:redundancy` 给出的 **7** 是经过上下文过滤后的重复选择器；`check:styles` 给出的 **415** 是原始规则层面的重复计数。前者已排除 SCSS 嵌套子规则，后者仍是原始规则层面的观察值；它们不能作为同一个 KPI。

若不处理，会产生两类问题：

- 维护者以为样式回退严重，实际可能是响应式或状态规则的合理重复。
- 真正危险的跨模块覆盖被大量原始计数淹没，无法排优先级。

**处理原则：** 保留两个指标，但改为明确名称、分别定义阈值和行动方式。

### P1：样式入口导入过多，顺序即隐性依赖

文章、聊天、数字人、音乐等页面导入 12～19 个 CSS/SCSS 文件。现状已能通过归属检查，但任何新增“覆盖”文件都可能依赖导入顺序，进而出现局部修复、跨页回归。

**处理原则：** 由功能目录提供单一 `styles/index.css` 或 `featureStyles.css` 入口，并以统一层级声明顺序；页面 JSX 只导入该入口。

### P1：大页面同时承担业务编排、视觉结构和状态分支

大型 JSX 会导致样式修改需要理解任务轮询、弹窗、上传和历史等不相关逻辑，视觉回归难以只验证一个区域。

**处理原则：** 先按可独立渲染的视觉区域拆 React 组件，再让每个区域拥有就近样式模块；保留页面文件作为数据流和区域编排器。

### P1：公共样式边界仍需硬化

`components.css` 和 `appShellFeature.css` 仍较大。它们应只承载公共组件、布局原语和跨功能状态，不应变成业务页面的“最后覆盖层”。

**处理原则：** 公共层只允许通用命名空间 `.ui-*`、`.app-*`；任何 `.article-*`、`.chat-*`、`.assets-*` 等业务选择器都回到 feature 目录。

### P2：39 个样式文件含有 legacy / override / compat / temporary / todo 等过渡性命名或注释

这不代表它们必须删除，但说明迁移轨迹仍然可见。长期保留会使开发者继续通过“再加一层覆盖”来修复样式。

**处理原则：** 为每一处过渡规则归类为“可合并、需保留兼容、待删除”；没有明确理由和到期条件的规则不得继续保留。

### P2：静态媒体资源体积会放大首屏与部署成本

资源清单约 2.04 GiB，其中视频约 1.70 GiB。此项不只是运维问题：页面展示背景视频、预览视频时会直接影响加载、滚动和交互感知。

**处理原则：** 先建立引用清单与资源分级，再做格式、尺寸、懒加载和 CDN 迁移；任何删除须有静态检索、运行时路由冒烟和发布验证。

### P2：视觉测试已覆盖关键路径，但尚未形成“状态清单”

当前已有标准五档宽度和多个历史空状态的回归测试。下一步应将每个高频工作台的 `empty / editing / processing / success / error` 明确纳入页面状态矩阵，而不是依赖新增功能时临时补截图。

---

## 4. 目标样式架构

### 4.1 层级与导入顺序

建议形成以下固定层级。新增样式不得跨层反向覆盖：

```text
1. tokens        色彩、间距、圆角、阴影、层级、动画时长
2. reset/base    元素默认值、可访问性、全局排版
3. layout        App shell、页面容器、栅格与响应式布局原语
4. components    Button / Dialog / FeedbackState / HistoryEmptyState 等通用组件
5. features      article / chat / assets 等业务模块
6. utilities     极少量显式工具类；禁止放业务补丁
```

实现要求：

- `src/main.jsx` 仅导入 1～4 层的全局样式。
- 每个 feature 的页面 JSX 只导入一个本功能样式入口，例如 `./styles/index.css`。
- `styles/index.css` 只负责顺序编排，不放选择器。
- 优先使用 feature 根节点或已有页面根类限制作用域，避免裸 `.panel`、`.header`、`.empty-state` 影响其他页面。
- 不新增 `final.css`、`fix.css`、`override.css`、`temp.css` 一类文件；紧急修复必须在原职责文件中落位，并在同一 PR 中清理过渡规则。

### 4.2 令牌使用边界

当前检查已识别 317 个令牌。下一步不是追求令牌数量，而是减少“同义值多命名”和业务文件直接写视觉常量。

| 类别 | 推荐命名 | 允许位置 | 示例 |
| --- | --- | --- | --- |
| 颜色 | `--color-text-primary` | token 层 | 主文本色、状态色、边框色 |
| 间距 | `--space-1` ～ `--space-8` | token 层 | 容器内外边距、栅格间隙 |
| 圆角 | `--radius-sm/md/lg` | token 层 | 卡片、输入框、弹层 |
| 层级 | `--z-header/modal/toast` | token 层 | 固定头部、对话框、通知 |
| 动效 | `--motion-fast/base` | token 层 | hover、载入、过渡 |

例外：媒体宽高、裁切比例、业务预览舞台尺寸可保留在 feature 中，但必须以组件职责命名，例如 `--video-stage-min-height`，不能使用无意义的 `--height-1`。

### 4.3 公共状态组件的边界

已存在的 `HistoryEmptyState` / `FeedbackState` 应继续作为空、加载、错误反馈的第一选择。

- 公共组件负责：插画、标题、居中、基础最小高度、无障碍语义和统一间距。
- feature 负责：业务容器尺寸、列表/表格外壳、特定操作入口。
- 不在每个页面重复输出插画 `<img>`、图标 + 标题 + 描述结构。
- 如有确实需要的操作按钮，新增受控 `action` 插槽；不能为了一个例外复制一套空状态 DOM 与 CSS。

---

## 5. 分批改造清单

### 批次 A：先校正度量与样式入口（优先级 P0）

#### A1. 明确检查脚本的两个“重复”指标

涉及文件：

- `frontend-app/scripts/check-redundancy.mjs`
- `frontend-app/scripts/check-style-system.mjs`
- `frontend-app/package.json`
- `frontend-app/README.md` 或 `frontend-app/docs/style-governance.md`（新增）

改造项：

1. 将输出字段更名为：
   - `contextualDuplicateSelectors`：同一作用域、同一层级下的可行动重复；SCSS 嵌套子规则不单独计数；以 7 为当前基线。
   - `rawSelectorOccurrences`：原始规则出现次数；仅做趋势观察。
2. 在脚本输出和文档中写清排除项：`@keyframes`、SCSS 嵌套父选择器、合法响应式覆盖、状态变体。
3. 为 `contextualDuplicateSelectors` 保留门禁，例如不高于 45；原始指标不作为阻断门禁，除非连续两个版本显著上升。
4. 增加 `--duplicate-details` 的使用说明，要求新增重复时能定位文件、选择器和行号。

验收：

```powershell
npm.cmd run check:redundancy
npm.cmd run check:styles
```

- 控制台中不再出现同名但口径不同的“duplicate selectors”。
- 任一新增的上下文重复可从输出直接定位。

#### A2. 为高扇出页面建立单一样式入口

第一批目标：

- `src/features/article/ArticleGenerationView.jsx`（19 个导入）
- `src/features/chat/ChatGenerationView.jsx`（15 个导入）
- `src/features/digital-human-v2/DigitalHumanV2View.jsx`（12 个导入）
- `src/features/music/MusicGenerationView.jsx`（12 个导入）
- `src/features/assets/AssetsPage.jsx`（9 个导入）

建议目录模式：

```text
src/features/article/
  styles/
    index.css                 # 仅 @import，固定顺序
    article-shell.css
    article-composer.css
    article-results.css
    article-history.css
    article-template-preview.scss
    article-responsive.css
```

步骤：

1. 保持现有样式文件内容不变，先新增入口文件并按“shell → feature → state → responsive”排序。
2. 页面 JSX 删除多条样式导入，只保留 `import "./styles/index.css";`。
3. 如果有不属于该 feature 的公共样式，将其移至 `main.jsx` 或公共组件自身入口，不从页面间接引入。
4. 每完成一个 feature，使用 `rg` 确认页面内只存在一个 CSS/SCSS 导入。

验收：

- 上述页面各自的样式导入数降为 1。
- `npm.cmd run check:coverage` 继续通过。
- 打开页面时 DOM、布局和样式不发生变化；以截图测试比对。

风险与规避：

- 风险：`@import` 顺序改变造成局部覆盖失效。
- 规避：先只做等价的导入重排；单页面、单提交、单独视觉回归，不同时改选择器。

### 批次 B：收敛大型样式文件（优先级 P1）

#### B1. 视频换脸和视频工作流状态

目标文件：

- `src/features/face-swap/FaceSwapWorkbench.css`
- `src/features/video-workflow/videoWorkflowStates.css`

建议拆分：

| 新文件 | 职责 |
| --- | --- |
| `faceSwapLayout.css` / `videoWorkflowLayout.css` | 页面壳、双栏/单栏布局、内容区尺寸。 |
| `*Composer.css` | 上传、参数表单、操作按钮。 |
| `*Preview.css` | 视频舞台、媒体比例、遮罩、加载层。 |
| `*Results.css` | 成功结果、下载/重试/复制动作。 |
| `*History.css` | 历史卡片及与 `HistoryEmptyState` 的容器关系。 |
| `*Responsive.css` | 五档标准宽度的覆盖规则。 |

特别检查：视频预览容器必须明确 `aspect-ratio`、`object-fit`、`overflow` 和最小高度，避免选择形象或切换历史后出现媒体拉伸、超出舞台或双滚动条。

#### B2. 资产中心与首页

目标文件：

- `src/features/assets/assetsBilling.css`
- `src/features/home/homeApp.css`
- `src/layouts/appShellFeature.css`

改造项：

1. 账单、积分、记录列表的筛选栏和状态标签抽到资产 feature 内部共享样式，不进入全局 `components.css`。
2. 首页展示卡片与营销区块采用明确的根命名空间，防止同名 `.card`、`.title` 与工作台冲突。
3. `appShellFeature.css` 仅保留侧栏、顶部导航、主内容区等壳层规则；功能页面补丁移回各自 feature。

验收：

- 没有使用页面特征类名的规则留在 app shell 文件。
- 在 1024px 以下，主内容区只有一个纵向滚动来源。

#### B3. 聊天和文章工作台

目标文件：

- `src/features/chat/chatLayoutOverrides.scss`
- `src/features/article/articleWorkbenchBase.css`
- `src/styles/components.css`

改造项：

1. 对所有 `override` 规则逐条归因：属于 chat shell、message list、composer、modal 或第三方组件兼容。
2. 将可以回归原模块的覆盖规则移回相应文件；无法消除的第三方兼容规则放入明确命名的 `vendor-compat.css`，并在文件头标明依赖组件、原因和删除条件。
3. `components.css` 只保留 `HistoryEmptyState`、`FeedbackState`、通用按钮/提示等真正跨功能的类；文章和聊天私有选择器迁出。

验收：

- `chatLayoutOverrides.scss` 的名称可改为职责名称，或只剩明确的第三方兼容规则。
- `components.css` 中不出现 `.article-*`、`.chat-*`、`.assets-*` 等 feature 前缀。

### 批次 C：拆分大型页面组件（优先级 P1）

拆分方式遵循“**页面负责数据流；区域负责渲染；Hook 负责状态与副作用**”。先提取纯展示区域，避免一次重写任务轮询和请求逻辑。

#### C1. 文章生成页

目标：`src/features/article/ArticleGenerationView.jsx`。

建议组件：

```text
article/components/
  ArticleWorkbenchHeader.jsx
  ArticleComposerPanel.jsx
  ArticleTemplateGallery.jsx
  ArticleTemplatePreview.jsx
  ArticleResultPanel.jsx
  ArticleHistoryPanel.jsx
article/hooks/
  useArticleGenerationTask.js
  useArticleTemplateSelection.js
```

执行顺序：

1. 提取无副作用的 header、模板预览、结果展示。
2. 提取表单区域，并通过受控 props 回传字段变化。
3. 最后提取任务轮询 Hook；保持 API 调用参数和取消逻辑完全等价。
4. 页面文件最终仅保留路由参数、顶层状态组合和区域编排。

完成标准：`ArticleGenerationView.jsx` 目标控制在约 500～700 行，且每个子区域可单独进行 empty / loading / success 状态截图。

#### C2. 资产、图片、聊天、数字人和音乐

| 页面 | 首批提取区域 | 不应在首批改变的逻辑 |
| --- | --- | --- |
| `AssetsPage.jsx` | 资产导航、作品网格、账单/积分区域、收藏区域 | 账户数据获取和路由映射。 |
| `ImageGenerationView.jsx` | 生成参数、结果画廊、预览弹层、历史面板 | 任务轮询协议与下载行为。 |
| `ChatGenerationView.jsx` | 会话侧栏、消息流、输入器、空/错误状态 | 流式消息拼接与中断逻辑。 |
| `DigitalHumanV2View.jsx` | 形象选择、音色选择、场景选择、预览舞台 | 生成请求字段和积分校验。 |
| `MusicGenerationView.jsx` | 创作表单、历史、播放器、歌词/封面区域 | 播放队列和任务状态机。 |

验收：

- 每页根组件的 JSX 行数下降至少 30%，但不以强制数字替代业务可读性。
- 页面私有样式与子组件相邻，公共样式不因拆分而膨胀。
- `npm.cmd run test` 与重点页面截图测试通过。

### 批次 D：过渡性样式清理与命名治理（优先级 P2）

审计发现 39 个样式文件含有 `legacy`、`override`、`compat`、`temporary` 或 `todo` 等字样。执行时建立一份清单，每一项必须有明确去向：

| 分类 | 处理方式 | 允许保留条件 |
| --- | --- | --- |
| 可合并覆盖 | 移回原职责文件，删除过渡选择器 | 无。 |
| 第三方兼容 | 收敛进 `vendor-compat.css` 并加文件头说明 | 依赖版本、问题链接/说明、删除条件完整。 |
| 灰度/迁移兼容 | 绑定功能开关或迁移截止版本 | 有明确下线日期和责任人。 |
| 注释待办 | 转入 issue 或本方案批次 | 不保留无限期 TODO。 |

命名规则：

- 文件按“功能 + 职责”命名，如 `article-results.css`、`chat-composer.css`。
- 根类按“功能 + 区域”命名，如 `.article-results`、`.chat-composer`。
- 状态使用 `is-*` / `has-*`，变体使用 `data-variant` 或明确 modifier；避免 `.active`、`.content`、`.panel` 这种跨页面泛名。
- 禁止通过选择器深度超过 4 层来提升优先级；需要更高优先级时先检查作用域与 DOM 边界。

### 批次 E：静态媒体与页面体验（优先级 P2）

目标不是立即减少文件，而是先保证“每个保留的资源都值得被交付”。

#### E1. 资源引用审计

1. 导出 `public/assets`（或实际静态目录）清单：路径、类型、字节数、最后修改时间、源码引用次数。
2. 分为：首屏必需、用户交互后加载、历史预览、未发现源码引用、构建/部署保留。
3. 对“未发现源码引用”的文件，再检查动态拼接路径、后端返回 URL、运营配置和线上访问日志；未完成这四项检查不得删除。

#### E2. 优化策略

| 资源类型 | 推荐策略 |
| --- | --- |
| 首屏海报/缩略图 | 生成 AVIF/WebP，多尺寸 `srcset`，预留原图回退。 |
| 视频预览 | 按场景提供 poster；使用 `preload="metadata"` 或交互后加载，避免默认下载完整媒体。 |
| 大型背景视频 | CDN 托管、可配置 URL、首屏降级为 poster。 |
| 历史卡片媒体 | 固定缩略图尺寸、懒加载、列表虚拟化（达到数据量阈值时）。 |
| 可删除候选资源 | 先移动到可恢复隔离目录或发布后观察窗口，不做不可逆删除。 |

验收：

- 新增资源在 PR 中注明用途、尺寸、格式和加载策略。
- `check:assets` 保留为报告门禁；当资源超过约定预算时必须解释原因。
- 视频工作台在慢网下先显示 poster/骨架，不阻塞表单交互。

### 批次 F：视觉回归和可访问性门禁（优先级 P2）

#### F1. 状态矩阵

为下列高频页面维护状态清单：

- 图像、视频、动作迁移、视频换脸。
- 去水印、智能抠图、画质提升。
- 语音合成、音色转换、转录、视频配音、AI 音乐。
- 文章、聊天、数字人、资产中心。

每页至少列出：

| 状态 | 必测内容 |
| --- | --- |
| Empty | 公共空状态插画仅一张、标题居中、无描述残留、无多余滚动。 |
| Editing | 表单聚焦、上传、禁用按钮、错误提示。 |
| Processing | 进度/骨架、取消或离开页面行为、媒体占位。 |
| Success | 结果比例、下载/收藏/重试动作、历史回写。 |
| Error | 错误文案、重试入口、布局不塌陷。 |

#### F2. 标准宽度与交互检查

保持且新增断言的宽度：`375 / 768 / 1024 / 1440 / 1920`。

除截图外，关键页面应增加：

- 主内容区没有横向溢出。
- 壳层与内容区不会同时出现非预期纵向滚动条。
- 键盘 Tab 可到达上传、生成、下载、关闭弹层等关键控件。
- 预览视频/图片具有可理解的替代文本或合理的装饰性标记。
- `prefers-reduced-motion` 下，非必要循环动画可被减弱或关闭。

建议命令：

```powershell
npm.cmd run check:coverage
npm.cmd run check:redundancy
npm.cmd run check:styles
npm.cmd run check:breakpoints
npm.cmd run build
npm.cmd run test
```

---

## 6. 建议执行顺序与提交边界

不要将所有页面和样式文件放在同一个改造提交中。建议按以下可回退批次推进：

1. **A1：指标语义与文档**：无 UI 变化，先消除团队认知歧义。
2. **A2：单一样式入口**：每次只迁移一个 feature，做等价重排和截图比对。
3. **B：大型 CSS 按职责拆分**：从视频换脸/视频工作流开始，优先解决媒体舞台和状态混合。
4. **C：大型 JSX 组件化**：先文章和聊天，再资产、图片、数字人、音乐。
5. **D：过渡命名清理**：在前述 feature 已稳定后逐个归档或删除。
6. **E：静态媒体治理**：先建立报告与加载策略，再依据发布验证处理候选资源。
7. **F：把新状态写入自动化回归**：每完成一个 feature 的结构改造就补齐相应状态矩阵。

每个提交只允许一种主类型变更：

- 仅样式入口整理；或
- 仅一个功能样式文件职责拆分；或
- 仅一个页面区域组件化；或
- 仅测试/门禁改动。

这样出现视觉回归时可快速定位，不需要在大量重构与样式变化之间猜测原因。

---

## 7. 完成定义（Definition of Done）

以下条件同时满足，V4 才可视为完成：

- [x] 两种重复选择器指标有独立名称、口径、阈值和说明文档。
- [x] 文章、聊天、数字人、音乐、资产五个高扇出页面各只保留一个样式入口导入。
- [x] 上述大型样式文件已按职责拆分，且没有新增超过 800 行的样式文件。
- [x] `components.css` 与 `appShellFeature.css` 不再承载 feature 私有选择器。
- [x] 过渡性样式文件/注释均有去向；无原因的 `final`、`override`、`temporary` 文件为 0。
- [ ] 历史/结果空状态仍统一使用 `HistoryEmptyState`，不重新出现单页插画和说明文案副本。
- [ ] 每个重点工作台至少覆盖 empty、processing、success、error 中的关键可达状态，并覆盖五档标准宽度。
- [x] `check:coverage`、`check:redundancy`、`check:styles`、`check:breakpoints`、`build`、`test` 均通过。
- [ ] 静态媒体完成引用分级和加载策略说明；没有未经审计的批量删除。

---

## 8. 改造时的禁止项

1. 不以“再加一个覆盖文件”修复视觉问题。
2. 不用 `!important` 绕过作用域或导入顺序问题。
3. 不以删除现有样式文件的方式绕过 coverage 检查。
4. 不把 feature 私有样式塞回 `components.css`、`appShellFeature.css` 或全局入口。
5. 不在 CSS 中重新增加 375 / 768 / 1024 / 1440 / 1920 以外的媒体断点。
6. 不根据文件名或一次 `rg` 结果直接删除静态媒体。
7. 不在同一个提交中同时重写页面请求逻辑、重排 DOM、拆 CSS 和调整视觉规范。

---

## 9. 预期收益

完成后，前端样式体系将从“已能通过基本门禁”提升到“变更范围可预测、覆盖关系可追踪、视觉状态可回归”：

- 新页面不再依赖临时 CSS 覆盖完成开发。
- 历史/结果/空状态保持一致，并继续复用公共组件。
- 复杂页面的局部样式调整不必理解整页业务状态机。
- 断点、空状态、媒体预览和滚动问题可在自动化中尽早发现。
- 大型媒体资源具备安全的降载和迁移路径，不会因误删影响线上功能。

---

## 10. 实施状态（2026-07-25）

已完成并经构建/视觉回归验证：

- 样式覆盖、重复选择器、标准断点、单样式入口、包体积和静态资源审计门禁。
- 高扇出页面单入口：文章、聊天、数字人、音乐、资产、图像和换脸。
- 历史记录空状态统一为 `HistoryEmptyState`，避免页面私有插画和描述副本；`check:history-empty-states` 已纳入构建前门禁，历史“暂无/正在加载…记录”标题不得脱离该公共组件。
- 文章工作台已拆出表单面板、模板弹层、全屏预览媒体、历史预览弹层和预览工具函数。
- 聊天工作台已拆出 `ChatConversationCanvas`，页面容器只保留会话与流式请求状态机。
- 图像生成已拆出标签与图库展示区域；资产页已确认维持画廊、收藏、账户和账单子视图边界，并补充空画廊视觉回归基线。音乐生成页已将创作表单/最近生成区及历史列表提取为 `MusicReferenceLayouts`，页面容器只保留任务轮询、播放器、下载与状态编排；音乐默认、生成、完成播放器四项视觉回归均通过。数字人页面已将侧栏、形象库和预览舞台组合抽离为 `DigitalHumanWorkspace`，场景上传控件也移入该展示层；生成请求、任务更新、草稿和弹层状态仍由页面容器管理，五项数字人视觉回归通过。文章页已将平台、模板与默认值迁至 `articleWorkspaceConfig`；历史任务网格抽离为 `ArticleHistoryGrid`，完成/处理中/失败三态卡片和公共空态均由该组件负责；左侧三步编辑器已抽离为 `ArticleFormPanel`，文案草稿、模板预览和最终结果舞台已抽离为 `ArticleResultStage`，主页面继续管理任务请求、预览弹层和确认操作。
- 静态资源审计已按直接引用、动态目录引用和源码未引用分类：动态灵感目录约 1.85 GiB，不能按字面量搜索结果删除；当前源码未引用候选为 84 个、约 10.1 MiB，仍需结合后端和部署引用复核。非自动播放的数字人草稿/结果预览及照片数字人结果视频已显式使用 `preload="metadata"`，避免预览默认下载完整媒体。
- 视觉回归覆盖已补充资产空画廊；聊天流式加载态的模拟时长已固定，避免加载态与完成态竞争导致快照不稳定。语音合成、音色转换与转录历史页已补充空态及完成态快照；图片收藏图库已补充真实图片加载断言及完成预览快照；图像工作台已补充处理中与失败态快照，验证历史列表、状态提示和失败重试入口；文章历史已补充 processing / completed / failed 三态网格快照；文章工作台另增加真实草稿提交流程，覆盖抽离后的平台、内容类型和版式配置；照片复刻历史页已补充公共空状态与完成结果快照。数字人已补齐真正经过“试听→确认→生成”链路的处理中、完成和失败态快照：完成态验证真实视频预览及重新生成、下载、删除操作区，失败态验证公共空状态且没有成功操作区，避免把试听确认前的草稿画面误作为处理中。当前单元测试和 62 项路由/视觉测试均通过；历史相关用例连续四轮共 64 项均通过。
- 五档标准宽度的横向溢出断言已扩展到图像、视频、换脸、动作迁移、数字人、音乐、文章、聊天和资产页；当前构建、样式门禁、单元测试与路由/视觉测试均通过。
- 文章工作台的 `final-*` 过渡样式文件已重命名为布局、视觉选项、模板轨和响应式职责名称；导入顺序与视觉快照保持不变。
- 聊天历史栏样式已从历史终态文件收敛为 `chatHistoryTreatment.css`；空、加载、完成和错误状态快照均通过。
- 文章工作台样式目录已移除历史行号和 `residual` 命名，18 个样式文件均以职责命名；组合入口与组件直接引用已同步，覆盖检查、文章视觉回归和构建均通过。
- 视频生成历史已新增空态与任务状态快照：空态使用 `HistoryEmptyState`，任务列表覆盖 `processing`、`completed` 和 `failed` 三种服务端状态。动作迁移与视频换脸共享的视频工作流历史也已验证相同三种任务状态。
- 去水印、智能抠图和画质提升的历史页已补齐状态快照；三页均使用公共空状态，去水印的“暂无生成记录”已统一为“暂无历史记录”，并验证 `processing`、`completed`、`failed` 任务卡片。
- 视频配音历史页已补齐空、处理中、完成、失败四态快照；处理中卡片显示当前阶段与进度，成功和失败态的可用操作保持明确，加载图标遵从 `prefers-reduced-motion`。语音合成、音色转换与转录的历史接口只持久化完成结果，因此仅为实际可达的空态与完成态建立基线，不伪造历史中的处理中或失败卡片。
- 聊天、音乐、资产、灵感抽屉和数字人相关的纯 Sass 样式已迁移为标准 CSS；数字人中心页的一处嵌套选择器已显式展开。文章入口经过视觉对比后确认不能机械转换，暂保留受控 Sass 入口以维护既有级联顺序。
- 历史/结果页空状态已完成源码审计；资产卡片网格中未被消费的图标与描述参数已移除，避免与“空状态不显示说明文案”的规范冲突。
- 语音结果画布已合并末尾重复覆盖规则；桌面与移动端的最终计算样式不变。视频工作流已移除 768px 断点中被后续规则完全覆盖的三列时间线声明；首页展示导航与根容器已合并分散的基础规则；音乐迷你播放器已移除被同断点完整定义覆盖的网格规则；去水印页已清理永远隐藏的结果网格尺寸和被完整规则覆盖的图标声明；图片结果预览与视频结果卡片已分别移除重复定位和被覆盖的视觉声明；聊天侧栏已将最终轨道、历史列表和操作栏规则收拢为单一入口；爆款图文模板卡片已将分散的按钮规则收拢为单一入口；文章历史卡片与 1024px 工作台布局已合并被末尾规则完全覆盖的声明。文章受控 Sass 入口的相邻结果卡片规则也已合并。冗余检查现已排除 SCSS 嵌套子规则误报；上下文重复选择器由 35 降至 6。

仍需继续：

- 图像生成已提取标签和图库展示区域；资产页已保持画廊、收藏、账户和账单的子视图边界，后续只在出现新的展示职责时再提取，避免无收益的拆分。
- 文章 Sass 入口仍需在可复现编译规则顺序的前提下迁移；不得以原生 CSS `@import` 或 Sass `@use` 机械替换。后者在 2026-07-25 的文章空态快照中产生约 3% 像素差异，恢复原顺序后三项文章回归均通过。

## 11. 完成审计（2026-07-25）

| 完成定义 | 审计结论 | 当前证据 / 缺口 |
| --- | --- | --- |
| 重复选择器指标 | 已验证 | `check:redundancy` 定义上下文重复选择器口径，当前为 6，阈值为 45，并排除 SCSS 嵌套子规则误报。 |
| 高扇出页面单样式入口 | 已验证 | `check:style-entrypoints` 覆盖文章、聊天、数字人、音乐、资产、图像和换脸页面。 |
| 样式按职责拆分 | 已验证 | `check:redundancy` 未发现超过 800 行的样式文件；文章、聊天、图像等已拆分。 |
| 全局样式不承载 feature 私有选择器 | 已验证 | `check:styles` 现在显式禁止 `components.css` 与 `appShellFeature.css` 中出现各 feature 前缀。 |
| 过渡命名治理 | 已验证 | 文件名搜索未发现无说明的 `final`、`override` 或 `temporary` 样式文件；保留的迁移注释均提供来源或职责。 |
| Sass 语义迁移 | 部分验证 | 除文章入口外，源码 Sass 已迁移为 CSS 且不再产生 Sass 告警；文章的原生 CSS 与 Sass `@use` 试迁移都会改变级联顺序并触发视觉回归，暂保留受控入口，待建立等价编译顺序后处理。 |
| 空状态统一 | 已验证 | 重点历史页已使用 `HistoryEmptyState` 并有视觉回归；去水印、智能抠图、画质提升的空态已统一为“暂无历史记录”。`check:history-empty-states` 已阻止新增历史记录标题绕过公共组件。 |
| 工作台状态矩阵 | 部分验证 | 已覆盖图像、聊天、音乐、文章、数字人、水印、智能抠图、画质提升、视频配音、视频工作流、视频生成、照片复刻及语音工具的关键状态；图像工作台、文章历史与数字人均已覆盖编辑及真正提交后的 processing / completed / failed。视频生成、动作迁移、视频换脸、去水印、智能抠图、画质提升、视频配音现已具备 empty / processing / success / error 基线；语音合成、音色转换、转录已覆盖其真实可达的历史空态/完成态基线，照片复刻已覆盖历史空态/完成态基线，其他重点工作台仍未全部补齐完整矩阵。 |
| 质量门禁 | 已验证 | 样式覆盖、冗余、样式系统、断点、入口、构建、单元和路由/视觉测试最近一次均通过。 |
| 静态媒体治理 | 部分验证 | 已完成引用分级与加载策略审计；84 个约 10.1 MiB 的源码未引用候选仍需后端与部署侧引用证据，未做未经审计的删除；非自动播放的数字人草稿/结果和照片数字人结果播放器已采用 `preload="metadata"`。 |
- 根据静态资源审计结果制定 CDN、poster、缩略图和懒加载迁移批次；审计完成前不批量删除媒体。

### 11.1 剩余重复选择器决策台账（2026-07-25）

`check:redundancy --duplicate-details` 当前报告 6 条上下文重复选择器。这些规则已逐类审计，不将其作为“为降低数字而删除”的候选：

| 分类 | 数量 | 当前结论 | 后续处理条件 |
| --- | ---: | --- | --- |
| 文章工作台受控 Sass 级联 | 4 | 表单、结果卡片和响应式规则跨多个受控入口叠加；已清理同文件内的后续覆盖，SCSS 嵌套子规则不再误计入指标。原生 CSS 与 Sass `@use` 试迁移均已产生视觉回归。 | 仅在可复现 Sass 编译顺序、补齐文章工作台与历史页截图后合并。 |
| 应用壳层响应式令牌 | 1 | `:root` 在不同标准断点重写布局令牌，属于合法响应式覆盖。 | 保留；新断点只能使用 375 / 768 / 1024 / 1440 / 1920。 |
| 工作室落地页脚视觉版本 | 1 | 页脚基础排版与桌面信息架构为不同展示层级，不适合按选择器名直接合并；当前 `getRouteView()` 默认落到 `home`，`StudioLanding` 分支没有可达路由。 | 待产品确认可达入口后，先补桌面/移动端视觉快照，再按区块重组；不以样式改动改变路由。 |

因此当前冗余门禁以 45 为上限、7 为已审计基线；任何新增项必须在同一表中说明它是合法状态/响应式规则，或在对应 feature 内合并。
## 媒体交付决策（2026-07-25）

- 当前阶段继续直接加载现有静态媒体资源，不迁移、不删除，也不以 OSS/CDN 接入为本轮优化的前置条件。
- 后续接入 OSS 时，视频采用 WebM 作为默认资源，MP4 作为兼容性兜底；页面应通过同一视频源配置维护两种格式，避免业务组件各自拼接资源地址。
- `frontend-app/scripts/audit-static-asset-references.mjs` 会按同一路径主文件名统计 WebM/MP4 配对情况，仅输出迁移候选报告，不将未配对资源视为当前错误。
- 在 OSS 配置、部署引用和回归验证完成前，禁止依据本地引用扫描结果批量移动或删除媒体文件。
### 历史空态收敛补充（2026-07-25）

- 去水印、智能抠图、画质提升的历史空态已移除 `watermark-recent-empty`、`remove-bg-recent-empty`、`enhance-recent-empty` 等页面私有类及其重复插画伪元素。
- 三页现在仅通过 `HistoryEmptyState` / `FeedbackState` 使用公共插画、标题、尺寸和居中布局；不再保留描述文案或页面私有标题颜色覆盖。
- 已更新去水印空态视觉基线，并通过去水印、智能抠图、画质提升对应的 Playwright 状态回归。
- 智能抠图与画质提升已新增历史空态视觉回归：断言 `HistoryEmptyState` 的插画、标题和无描述文案，并分别建立空态快照；去水印、智能抠图、画质提升三页现在均具备空态及 processing / completed / failed 基线。
### 文章 Sass 入口迁移补充（2026-07-25）

- `articleStyles.scss` 保留原有的 CSS 导入顺序，并将六个本地 Sass `@import` 替换为 `sass:meta` 的 `load-css` 原位加载；不再触发 Dart Sass 的 `@import` 弃用警告。
- 完整生产构建及文章工作台、提交、历史三态和快捷模板共五项 Playwright 回归均通过，证明该迁移未改变当前可见级联结果。
