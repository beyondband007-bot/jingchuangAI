# 单用户登录态架构改造总结

## 背景

本次改造先不实现真实登录、注册、Session、Cookie 和登录弹窗，而是把当前本地 Demo 固定视为一个已登录用户：`demo-user`。

目标是先把用户维度的数据架构打通，让后续接入真实登录时，只需要替换“当前用户来源”，尽量不再大改各业务模块。

## 已完成内容

### 1. 后端统一当前用户上下文

- 在 `backend/src/shared/userService.js` 中新增当前用户上下文能力。
- 新增 `attachCurrentUser` 中间件，所有 `/api/*` 请求都会自动注入 `demo-user` 到 `req.user`。
- 使用 `AsyncLocalStorage` 保存请求级用户上下文，旧模块即使还没有显式传 `userId`，也能从当前请求上下文取到当前用户标识。
- `backend/src/app.js` 已将 `attachCurrentUser` 挂载到 `/api` 路由前。
- `/api/me/credits` 已改为通过 `req.user.id` 查询当前用户积分。

### 2. 已有任务模块开始按用户上下文改造

- 图片、视频、图文相关链路已显式传入 `req.user.id`。
- 原来部分 repository 中直接使用 `DEMO_USER` 的地方，已改为从当前请求上下文读取用户标识。
- 当前行为仍保持为本地 `demo-user`，所以不影响现有 Demo 使用体验。

### 3. 补齐最近记录持久化表

新增了以下任务表，并已通过 `npm run db:init` 初始化：

- `music_tasks`
- `voice_synthesis_tasks`
- `voice_convert_tasks`
- `transcribe_tasks`
- `replicate_tasks`

这些表用于替代原先前端 `localStorage` 或后端内存数组作为“最近生成”的主存储来源。

### 4. 音频、转录、Replicate 模块改为服务端持久化

以下模块现在会在生成或分析完成后写入数据库：

- AI 音乐
- 语音合成
- 音色转换
- 音频转录
- Replicate 分析

同时新增或复用了对应的任务列表接口：

- `GET /api/music/tasks`
- `GET /api/voice/tasks`
- `GET /api/voice-convert/tasks`
- `GET /api/transcribe/tasks`
- `GET /api/replicate/tasks`

### 5. 前端最近记录改为优先读取后端

以下前端模块启动时会从后端读取最近记录：

- `frontend-app/src/features/music/MusicGenerationView.jsx`
- `frontend-app/src/features/voice/VoiceSynthesisView.jsx`
- `frontend-app/src/features/voice-convert/VoiceConvertView.jsx`
- `frontend-app/src/features/transcribe/TranscribeView.jsx`
- `frontend-app/src/features/replicate/ReplicateView.jsx`

旧的 `localStorage` 记录没有做迁移；从本次改造之后的新生成记录会写入数据库，刷新页面后仍可见。

## 兼容策略

- 当前 Demo 仍然无需登录即可使用。
- 现有 `demo-user` 的历史任务、积分和生成记录不迁移、不删除。
- 新增的持久化表只影响之后新产生的音乐、语音、音色转换、转录和 Replicate 记录。
- 原有 `/recent` 接口保留，新增 `/tasks` 用于后续统一任务列表语义。

## 验证结果

已完成以下验证：

- `backend npm run db:init` 成功。
- 后端 `app` 可正常导入。
- HTTP 烟测成功：
  - `/api/me/credits`
  - `/api/music/tasks`
  - `/api/voice/tasks`
  - `/api/voice-convert/tasks`
  - `/api/transcribe/tasks`
  - `/api/replicate/tasks`
  - `/api/image/tasks`
- `frontend-app npm run build` 成功。

## 后续接入真实登录时的建议

后续加入真实登录功能时，建议主要从以下位置继续：

1. 在 `users` 表上补充登录字段，如邮箱、手机号、密码哈希等。
2. 新增登录态存储，例如 `auth_sessions` 表或 JWT/Session Cookie。
3. 替换 `attachCurrentUser` 中当前固定读取 `demo-user` 的逻辑。
4. 保持服务层、repository 层继续通过当前用户上下文或 `req.user.id` 获取用户。
5. 逐步把所有模块都显式改为传入 `userId`，最终减少对 `AsyncLocalStorage` 的依赖。

## 注意事项

- 旧浏览器 `localStorage` 中的历史记录不会自动进入数据库。
- 如果确实需要迁移旧本地记录，可以后续额外做一次性导入接口。
- 当前还没有真实权限拦截，因为本轮目标是“单用户登录态架构”，不是完整登录系统。
