# CDN origin for files currently addressed under /assets. Leave empty while
# serving from the application origin; set it before excluding large videos
# from the Docker build context.
VITE_STATIC_ASSET_BASE_URL=https://static.example.com
